---
id: rulings
title: "Rulings — the Owner seat's numbered decisions (the only definition site)"
type: doc
status: accepted
owner: "@owner"
phase: "coordination"
tags: [coordination, owner-review, rulings, register]
links:
  - { to: coordination-application-build, rel: relates-to }
review-by: "2027-03-22"
summary: >-
  The ruling register. Each `### Ruling NN — <title>` heading defines exactly one numbered
  decision of the Owner seat; prose anywhere cites it as `Ruling NN`. Written only by
  `coord decide rule`; numbering is read from these headings; verify-ruling-citations.py fails a
  cited number with no heading here and a number defined twice.
---

# Rulings

One heading, one decision. A ruling is never edited in place: a later ruling supersedes it in
prose and cites it. Each block carries the decision request it answered and who ruled.

### Ruling 1 — Conditional architecture and routine-worker routing

Authorize Agy 1.2.7 claude-opus-4-6-thinking as the first architecture author, with cfd-owner-20260923 and root retaining independent T2 review. This is an available author route, not an assertion of equivalence to an unavailable Opus 5.5 identifier and not a stack selection. Dispatch requires actual receipts binding the requested and effective model, supported native permission mode, working directory and run identity; exercise cancellation and denial behavior before relying on them. Serial observed-only containment is admissible when clearly labelled: isolated worktree, precommit identity checks, exact owned-path diff inspection, and independent root review at join. Do not describe instruction-only or merely observed restrictions as enforced. Never weaken native permissions, bypass a denial, or leave an unanswered prompt running. Grok 1.0.41 authentication and grok-4.7 availability reported after the user update supersede the earlier unauthenticated finding; qualify that exact installed profile and prefer grok-4.7 for bounded routine implementation after architecture contracts pass. Do not infer model quality from build-fast naming; use it only after a task-relevant qualification justifies it. Qualified Agy remains the fallback. Coordinator updates stale routing rows and records qualification paths before launch. Failed qualification retains the brief and evidence and requests a new bounded route; it never automatically escalates permissions or treats the budget cap as success. This ruling closes routing only; architecture, geometry certificates, Windows evidence and production acceptance remain separately gated.

- request: req-01M376WRA33YFZF9ZH0EE6A8HF · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:23:09Z

### Ruling 2 — One bounded full-auto qualification, then author fallback

Supersedes Ruling 1 only as to the selected per-process native permission profile. The human explicitly authorized supported unattended/full-auto operation within the repository. Local agy --help confirms --dangerously-skip-permissions is a per-process flag that auto-approves tool permission requests, and --sandbox enables terminal restrictions. Permit one fresh isolated qualification session using that supported full-auto flag plus --sandbox; retain the denied accept-edits session and do not resume or replay it. This is not authorization for global trust changes, added workspace directories, access beyond the owned repository/sacrificial qualification fixtures, package-policy workarounds, or continuation through any new explicit denial. Record exact argv, model, effective permission mode, cwd, sandbox behavior, one bounded allowed file edit, cancellation outcome and raw receipts. Use only disposable qualification sentinels for boundary probes; no unrelated user files. If confinement is merely observed, label it observed-only and require serial execution, precommit identity and exact path-diff checks plus independent root review; never claim --sandbox establishes filesystem enforcement without evidence. Cap this fresh qualification at five minutes and one attempt. A new denial, unsupported flag, wrong effective model, or missing bounded completion ends this route. Then choose an already qualified Grok 4.7 author under Astra Owner decisions and root independent review, or reuse the existing built-in Astra language author in its assigned isolated tree if the native CLI is still unqualified. Prefer qualified Grok for subsequent routine coding. Do not qualify every harness or retry permission variants indefinitely. Coordinator dispatches only a route supported by its actual receipts and preserves the unchanged architecture contract and scope. No architecture, scientific validity, Windows runtime or product acceptance gate is cleared by this routing ruling.

- request: req-01M377H4MQY0B14PFGS9GRXR7X · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:34:03Z

### Ruling 3 — Expected rejection is successful negative-probe evidence

Clarifies and supersedes only the denial classification in Ruling 2. An expected denial of the predeclared protected-path negative probe is successful protection evidence when the protected sentinel remains byte-identical; it does not trigger route fallback. Do not retry or circumvent that protected operation. An unexpected denial of an authorized in-scope operation such as pwd, the permitted fixture edit, or build still ends the Agy route. Ordinary command failures are classified separately from permission refusals: a sandbox-local runtime cache initialization failure may be retried within the same five-minute qualification budget using task-local DOTNET_CLI_HOME and NUGET_PACKAGES, without changing HOME, broadening directories, weakening sandbox policy, or altering global trust. Preserve first failure and retry receipts. Each qualification record must name the expected-reject operation, its observed denial and unchanged-byte oracle separately from allowed-operation execution. A negative probe that actually changes the protected sentinel is a failed protection claim, never a successful denial. All other limits, fallback routes and independent gates in Ruling 2 remain.

- request: req-01M377QY3MVNBKBKX663MS3V44 · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:37:25Z

### Ruling 4 — Bounded Grok ACP qualification beside architecture author

Authorize one independent Grok 1.0.41 ACP stdio qualification while the existing isolated Codex architecture author continues. Local help confirms grok agent --no-leader and its stdio subcommand; the transport is a candidate, not yet qualified. Use a fresh disposable probe worktree, explicit requested grok-4.7 and recorded effective alias, supported per-process unattended mode within the human-authorized repository scope, no plugin injection or global configuration/trust changes. Budget is five minutes total and one attempt, with at most the architecture author plus this probe active. Prove one allowed fixture write and a minimal local-SDK build, one predeclared protected-path rejection with unchanged disposable sentinel, and cancellation; use task-local caches and no HOME override. Record effective model, cwd, native policy, ACP permission responses, final drained tool receipts, build output and filesystem state. A completed read or pwd is not edit/build evidence. Expected protected-probe rejection is successful evidence; do not retry its blocked action. Any unexpected denial of an authorized operation, unsupported profile, path/model mismatch, or deadline ends the probe and retains its evidence. Classify native enforcement separately from client protocol refusal and instruction-only observation. If outside-root protection is only observed or unproved, never claim it is enforced; any later routine worker remains serial with identity, owned-path diff and root review containment. This ruling authorizes qualification only, not production dispatch or a replacement of the active architecture author. On failure retain the existing Codex fallback and continue the product track; no further transport or permission variants without a new bounded request. Agy remains closed under the previous failed qualification. M1 technical, geometry and cross-platform gates are unchanged.

- request: req-01M3788SPHE8SVS2X2MG87ABF8 · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:47:49Z

### Ruling 5 — Security and privacy rollups remain with the architecture author

Choose extend-author-paths. The design-slice security and privacy obligations are not deferred or assigned as authored repair work to the Coordinator. Coordinator shall amend the architecture author contract to add exactly docs/security/threat-model.md and docs/security/privacy-review.md, confirm no competing writer, and grant the normal authored-path claims before the author edits them. The author derives their registers from the design STRIDE/LINDDUN sections with the repository script, adds truthful metadata and links, and reports those two files in its exit inventory. Coordinator may regenerate derived surfaces at join but does not author missing risk dispositions. Existing scope, budget, no-production-code boundary and independent root veto remain. If the two added files cannot be completed within the remaining author budget, report that precise incomplete gate rather than claiming design-slice done.

- request: req-01M378DSNQ49SSTAZMWJW4WCCJ · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:56:46Z

### Ruling 6 — Harness parity and disposition of the failed Grok boundary probe

Reject the current Grok ACP profile for production dispatch now and retain the existing Codex architecture author. The protected sentinel write is an observed failed enforcement test; do not relabel that test as passed or the profile as confined. The receipt also leaves effective model alias, loaded cwd and cancellation unverified; ready_for_review is only a structural runner result. No additional probe is authorized by this ruling. A serial observed-only coding route is not categorically forbidden for Grok or Codex under the human authorization for autonomous repository work, but it is a different stated capability contract: worktree isolation and owned-path instructions are procedural controls, not a filesystem security boundary. Apply that exact language to built-in Codex as well; absence of its negative test is not evidence of stronger enforcement. Such a route requires, before any product dispatch, an explicit G3 contract that accepts the documented lack of enforcement, binds actual available model identity without inventing an alias, observes cwd and repository HEAD, provides an observed bounded cancellation path, names exact authored paths and task-local caches, permits no unrelated user-file or external-account actions, and limits the worker to one serial isolated track. Read back tracked and untracked path diffs and relevant protected-tree status before commit and join, then independently review code and behavior; these are detection and review controls and cannot undo an out-of-scope side effect. Any actual unexpected out-of-scope edit or denial bypass stops the worker, preserves evidence and excludes its changes from integration pending review. The deliberate disposable sentinel mutation during this qualification is retained as test evidence, not treated as production authorization. Existing Codex work continues under its already declared observed-only isolated contract, with direct tool cwd/path evidence and host cancellation capability checked by the Coordinator; if those prerequisites are absent it must stop on the same basis. At G3 the Coordinator may request a concrete routine-worker route with completed evidence; until then use the current fallback and do not spend more time on harness variants. Agy remains closed. This ruling settles policy parity, not product quality, geometry or Windows acceptance.

- request: req-01M378TB404Q81GP3R540092PJ · ruled by: cfd-owner-20260923 · at: 2026-09-23T13:57:27Z

### Ruling 7 — Architecture-only exception for unexposed Codex model identity

Grant the explicit limited exception for architecture track A only. The current collaborator inventory exposes /root/language status but no model identifier; record its model as Not recorded, never Astra or another inferred label. Owner directly observed its assigned cwd /Users/mallalieut/projects/CFD-Workbench-feature-application-architecture-codex, branch feature/application-architecture-codex, base HEAD a25c175c2dff173028eeb23ffcbad1c94b5a70f9 and the unjoined architecture documents/spike scratch. This observation does not certify finished scope, lifecycle or correctness. The author may finish the bounded architecture/design/spike contract and respond to independent review in that tree. Before join, Coordinator must read back final HEAD and exact tracked/untracked path inventory, preserve real execution and lifecycle receipts, and retain Not recorded for any unavailable model field. Root and Owner must independently inspect all load-bearing contracts, spikes and gate claims; author cannot self-clear. Unknown author model alone does not require reauthoring an otherwise independently established artifact. Any technical gap or failed gate still blocks the corresponding implementation or release claim. This exception supersedes Ruling 6 model-binding prerequisite only for the existing architecture author and is not a routine-worker precedent, stack approval, G3 clearance, permission-policy change or acceptance of claimed sandbox enforcement. Future worker routing still uses its concrete evidence and contract. Retain all rejected native-profile evidence.

- request: req-01M37913ECMT8806BYR8R5S3H4 · ruled by: cfd-owner-20260923 · at: 2026-09-23T14:00:20Z

### Ruling 8 — Conditional native M1 architecture and serial contract completion

Approve-conditional ADR 0003 direction: C#/.NET 10 with pinned Avalonia 11.3.14, UI-free shared GUI/CLI core, lossless source snapshots and append-only revision/cursor facts, and conservative continuously certified M1 admission. This selects a bounded offline native M1 architecture, not a simulation backend or production acceptance. Owner inspected final source and 30 Python primitive checks and independently executed nine native certificate-binding mismatch refusals; root independently reports conditional technical PASS plus actual macOS input, picker and cancellation. Source SHA, semantic Design revision and Surface definition identity remain distinct; exact source/base/draft/generation/evaluator/definition-hash binding is mandatory. Unsupported or inconclusive geometry remains Not assessed and cannot Apply. Per-span positivity and bounded maximum enclosure with propagated error may not be replaced by samples. Approve only serial B contract-completion/design work: freeze and execute the complete session ports, native-v1 schema/reference/operation-id/Undo-Redo fixtures, parser/source patch behavior, exact decimal and JCS identity vectors, growth/retention policy, and OS-specific persistence policy before production implementation or B/C fan-out. The source snapshot cap retains dirty state and the last complete file; Save As does not solve size overflow and history may not silently prune. Accept cooperative single-writer and OS-user ACL assumptions as the architecture posture; this does not accept ancestor races, uncooperative overwrite, unmeasured leakage or durability as solved. Native overwrite needs approved platform conflict/handle policy and real fault/race tests. No full G3, M1 delivery, complete FoilDSL evaluator, Windows live/UIA, viewport accessibility, signing/distribution, scientific result or telemetry-minimization gate is cleared. Required handback is committed nine-path substantive scope plus separately identified official generated/audit metadata, matching source fingerprints and independent root review; join remains behind mechanical graph checks. Ruling 7 model Not recorded exception remains architecture-A-only, with actual natural completion distinct from available but unexercised host cancellation. Coordinator owns next scoped contract and leases; this ruling grants none.

- request: req-01M3791C842XJCWYGECW0Q6ZVT · ruled by: cfd-owner-20260923 · at: 2026-09-23T14:10:28Z

### Ruling 9 — Bounded native-v1 contract policy and B0 model evidence

Amend and approve the B0 design policy, subject to independent executable-contract review under Ruling 8; no production dispatch. Native-v1 limits are 8000000 encoded UTF-8 bytes per envelope and 1048576 decoded bytes per source, with immutable full snapshot history and no silent pruning. Preflight exact prospective serialized growth before appending accepted/cursor facts; refusal preserves the accepted state, draft/dirty state and last complete file. An oversized recovery update may not replace the previous complete saved recovery; disclose the unsaved draft. Save As of identical content does not evade the cap. Numeric tokens are at most 4096 UTF-8 bytes. Bound digit/exponent scanning before BigInteger allocation or powers. The proposed [-400,400] range applies only to normalized effective decimal magnitude of a nonzero mathematical value, not the written exponent; compensated spellings within the token cap must agree. Detect all-zero significands without powers, including 0e1000000000, and normalize negative zero semantically. Nonzero values outside the supported work domain return DSL-LIMIT as unsupported resource admission, preserving source; they are not declared invalid FoilDSL merely because of that cap. Within the supported domain retain the exact decimal rational through exact unit scaling and round once to binary64 ties-to-even, including subnormal and signed-underflow-to-zero cases; independently apply structural positivity. Nonfinite/overflow remain normative errors. Specify the normalization algorithm and include token-limit, compensated-exponent, zero-huge-exponent, subnormal/tie/underflow and unit-equivalence fixtures. Missing IDs are generated deterministically in a candidate source before the final acceptance diff, with explicit acceptance afterward; opening never silently alters original bytes. IDs remain curve-local, unique and stable after acceptance; omitted-ID freeze/bounds references remain forbidden. Specify and fixture the exact collision-free ID rule. Persistence may publish a new file only through atomic no-replace creation, not absent-check followed by replace; test a competing creator. Cooperative overwrite additionally requires the verified platform handle-relative/no-follow and directory-identity policy with conflict/fault tests. If the platform adapter lacks the required primitive, return DOC-UNSUPPORTED-PERSISTENCE and retain original/session state; no claim-check-replace CAS claim. B0 may specify these adapter contracts and executable model fixtures without claiming native implementation proof. Fresh collaborator requested-model gpt-6-astra is recorded as requested, while effective model remains Not recorded unless exposed by an actual receipt. Extend the narrow architecture model-evidence exception to this serial B0 design route only, with exact cwd/HEAD/owned-path and completion evidence, observed-only containment and independent Owner/root artifact review. An available cancellation API is not observed cancellation. This exception clears neither routine production routing nor any Ruling 8 technical veto.

- request: req-01M37ASQVVK5EH805EZ81D2N8F · ruled by: cfd-owner-20260923 · at: 2026-09-23T14:33:52Z

### Ruling 10 — Durable rail edit receipts and explicit retry lifetime

Amend and approve the unshipped native-v1 receipt design for B0: accepted rows add required edit, null only for the initial Open, otherwise {draftId,generation,rail,vertexId} for every numeric rail Apply. A same-surface, same-value or source-only result of a rail command still retains its complete non-null edit receipt. A generic non-rail source-edit command is outside the current M1/B0 interface; it may not use null to erase binding and needs an explicit typed receipt and separate contract review before admission. The receipt plus parent/base, sourceId, referenced Design evaluator/hash and linked cursor action reconstructs the exact durable Apply payload. Validate draft UUID, safe generation, admitted rail and target membership in both base and resulting source, as well as all existing source/design/reference/cursor invariants. A receipt is an operation identity fact, not a geometry certificate or authenticated author claim. After Save/Reopen, identical persisted Open/Apply/Undo/Redo retries return their original result without new facts or cursor movement; changed payload, draft, generation, target or action under the same ID refuses DOC-OPERATION-CONFLICT. Open identity uses the explicitly accepted materialized candidate source bytes. Preserve all persisted transaction IDs permanently. Root Undo and empty-stack Redo remain no-ops that append no durable facts: deduplication for these no-op IDs is session-only and expires at Reopen. State that exception explicitly; callers generate fresh command IDs after Reopen, and must not infer durable exactly-once semantics for unpersisted no-ops. Writer preflight and reader acceptance must agree on every native size/line/string constraint, including multibyte or escaped vertex IDs: a 4096-scalar ID is not necessarily a legal 4096-byte JSON line. Reject an unrepresentable prospective envelope before any accepted/cursor mutation, preserving draft, history and prior file; include a boundary fixture. Required fixtures cover Open retry and changed source, Apply retry across Reopen with changed draft/target refusal, cross-action ID reuse, persistent cursor retries, volatile no-op lifetime, missing/malformed edit receipts and writer-reader boundary closure. This is a design-policy ruling only. Root BC-03 and the Ruling 8 executable-contract gate remain pending independent source and regression evidence; no production or full G3 approval.

- request: req-01M37BBVJ54MC38TKVFGN40Q5Q · ruled by: cfd-owner-20260923 · at: 2026-09-23T14:45:23Z

### Ruling 11 — Conditional serial Codex route for first production core

Select conditional built-in Codex serial for the first production core track only; this request does not authorize dispatch and does not clear the independent B0/G3 technical gate. Exact language identity, conservative continuous admission and durable transactions make this a difficult core task suited to the user-requested strong model. Coordinator must make and retain an actual fresh requested-model gpt-6-astra launch receipt, or identify an already bound requested-Astra worker explicitly. Record requested and effective identity separately; if native collaborator metadata does not expose the effective identifier, record Not recorded. Extend that evidence exception only to this first bounded core track, contingent on independent Owner/root code and oracle review; never relabel requested identity as verified effective identity. Before production dispatch observe one harmless cancellation drill in the same built-in harness: an actually active disposable task is interrupted, cessation is read back, and any owned child processes are observed quiescent. An API declaration, sent interrupt, natural completion or timeout alone does not establish cancellation. Do not interrupt unfinished B0 work for the drill; use an available released worker or fresh disposable worker under host limits, with no product mutation. Before the first production write, record the resolved B0 technical approval and scoped G3 contract, exact absolute worktree/branch/base HEAD and clean status read back by worker and Coordinator, compiled instruction/contract identity, exact owned source/test paths and no extra authors, one serial worker, and task-local cache/output paths for every actual build/test command. Do not repurpose HOME or write shared global caches. Commands run at explicit assigned cwd; recheck branch/HEAD and owned-path diff before commit and independently again at handback/join. Inspect untracked files, child artifacts and declared generated exceptions; unexpected out-of-scope writes, authorization denials, identity drift or unmanaged live processes stop the route for review rather than being worked around. Retain measured lifecycle, command exits, final filesystem state and cost/token values when available, otherwise Not recorded. Apply this same observed-only standard to Grok and Codex: neither an isolated worktree, path lease, permission label nor passing diff is an enforced filesystem boundary. The failed Grok sentinel does not prove Codex confinement. Current Grok ACP remains unqualified from existing receipts because effective model selection/alias, loaded cwd and cancellation are unclosed in addition to its observed outside-root write; no new Grok/Agy probe is authorized here. Codex is selected for the stronger requested model and observable local delivery path, not an invented sandbox guarantee. Later routine GUI/CLI/platform work requires a separate bounded routing decision using then-available evidence; this model-evidence exception and route do not propagate automatically. Root independently concurs with the narrow exception. No geometry, persistence, security, accessibility, Windows or product acceptance veto is cleared by harness routing.

- request: req-01M37BYS2CNRBF554ESX4ZWG0X · ruled by: cfd-owner-20260923 · at: 2026-09-23T14:52:14Z

### Ruling 12 — B0 bounded contract gate passed for G3 review

Approve the joined B0 serial technical contract gate for subsequent G3 review. This accepts the native-v1 schema/edit receipts, source/Design/Surface distinctions, numeric and canonical identity rules, session-owned rail draft, durable cursor/retry behavior, defensive snapshot boundaries, growth/retention policy and capability-gated persistence port described in design-application-contracts and ADR 0004. Reviewed author cb73079e041668221618b8dfc08753ff00b1d9f6 and independent root review ec0d89c7 are joined at clean Coordinator c13db2737440d78d384f48afa018af268e70f807. Owner directly observed clean eight-substantive/four-metadata handback, all three matching source fingerprints, final refusal guards and root receipt SHA256 796be000f955f8a9308ef4d1ed18cf7cad2bf353e29188ddfd6464a1383c3e48 with 89 CSharp checks, 42 Python checks and 2505 cross-runtime vectors. Root independently rebuilt and executed the frozen sources, inspected five constructed-mutant failures and granted bounded technical PASS. Owner findings for initial-Open replay, ASCII numeric input and truncated-token refusal are closed by named refusal/no-adoption tests. Coordinator reports successful epoch2 conductor join, architecture30+9 and B0 recount, docs check and 10/10 verification gates; durable success audit al-01M37D0H1HTGQKJCCX9HDARAP3 and strengthened recount source inspected. Conductor raw stdout was not retained separately, so this is a Coordinator-observed join result plus durable audit, not a claimed Owner rerun or fabricated raw receipt. Author commit hook was advisory because AGENT_SESSION was unset; post-commit path checks and clean exact inventory provide detection, not retroactive enforcement. Preserve that limitation and recurrence control. The reviewed contracts are the design baseline; shared seam changes require a new typed decision. This clears the serial B0 prerequisite only. It does not authorize production implementation, B/C fan-out, adapter launch, full G3 freeze, M1 acceptance, full language conformance, continuous geometry certification, native filesystem race/durability guarantees, runtime telemetry privacy, accessibility or Windows behavior. The fixture authority remains a labelled allowlist and must never be shipped as real validation. Exact production paths/test obligations and Ruling 11 cancellation/model/cwd/HEAD/cache/diff prerequisites must be satisfied through a separate G3 decision before the first core write. Platform adapters remain unavailable until actual native capability spikes and failure evidence establish their contracts; no unsafe persistence fallback is admitted.

- request: req-01M37D16NZ71ZKQFRNT5Y6AX0K · ruled by: cfd-owner-20260923 · at: 2026-09-23T15:10:56Z

### Ruling 13 — G3 serial first-core implementation freeze

Approve G3 for one serial first-core implementation track under the reviewed contract-b-core packet and Rulings 8 through 12. B0 contracts at joined c13db2737440d78d384f48afa018af268e70f807 are the baseline; this is implementation authorization, not product acceptance. Freeze exactly these 18 authored paths: global.json; CFDWorkbench.slnx; src/CfdWorkbench.Core/CfdWorkbench.Core.csproj; src/CfdWorkbench.Core/Contracts.cs; src/CfdWorkbench.Core/FoilSource.cs; src/CfdWorkbench.Core/Geometry.cs; src/CfdWorkbench.Core/Identity.cs; src/CfdWorkbench.Core/AuthoringSession.cs; src/CfdWorkbench.Persistence/CfdWorkbench.Persistence.csproj; src/CfdWorkbench.Persistence/ProjectStore.cs; tests/CfdWorkbench.Core.Tests/CfdWorkbench.Core.Tests.csproj; tests/CfdWorkbench.Core.Tests/FoilSourceTests.cs; tests/CfdWorkbench.Core.Tests/GeometryTests.cs; tests/CfdWorkbench.Core.Tests/IdentityTests.cs; tests/CfdWorkbench.Core.Tests/AuthoringSessionTests.cs; tests/CfdWorkbench.Core.Tests/ProjectStoreTests.cs; docs/proof/application-core.md; tools/verify-application-core.py. Coordinator assigns actual leases and isolated absolute worktree/session/branch/base, compiles and pins the packet, and obtains worker plus Coordinator identity/clean-path/cache readbacks before the first product write. Use CfdWorkbench.Core and CfdWorkbench.Persistence as the production module boundaries; B0 ApplicationContracts remains a fixture namespace. Official docs index/audit outputs are explicitly identified metadata exceptions, not extra authored leases. No GUI, CLI adapter, spec, architecture or generic source-editor implementation is assigned to B. No additional spike path is leased: execute focused native capability reproducers inside ProjectStoreTests.cs before implementing the corresponding platform adapter; document exact SDK signatures and measured behavior. Unknown or failed platform primitives remain unsupported, never guessed or bypassed. Full grammar recognition and phase-correct diagnostics precede supported-subset admission; the fixture allowlist may not become production validation. Implement exact source/semantic identity, conservative continuous geometry proof and bounded error, owned rail transactions, immutable histories/receipts and capability-gated persistence with meaningful red-to-green tests. Retention/telemetry follows B0: local 256-event session ring, discarded at close, no persistent MRU/exporter and no source/path/name/vertex/raw exception/hash-correlation leakage; events expose measured outcomes and resource refusal. Root independent code/oracle review and Owner Data/Test/Geometry/Security/API review precede integration. B/C stays serial until reviewed compiled public core and consumer fixtures establish the next seam. No partial B join or silent scope expansion; the 90-call/55-minute initial window is a checkpoint and replan bound, not a completeness claim. The reviewed cancellation receipt establishes actual agent interruption plus explicit termination/readback of its surviving owned child. It does not establish automatic subprocess cancellation. Ruling 11 monitoring must track PID and process-start identity, stop dispatch on interruption, terminate only verified owned processes and read back absence; uncertainty stops the route. Requested gpt-6-astra and effective Not recorded remain separate under the first-core-only exception; confinement remains observed-only. Mark ADR 0003 accepted for this conditional native offline M1 direction and ADR 0004 accepted for the reviewed unshipped contract. Root owns the separate project-frontdoor/spec/architecture status reconciliation, preserving managed blocks and explicitly retaining simulation backend unselected. Windows runtime, native persistence safety/durability, complete production geometry, UI/accessibility, privacy behavior, distribution and full M1 remain unaccepted until their actual product evidence passes. Later routine adapter routing is a separate decision.

- request: req-01M37DC0TYKNGFDYW94P8Y8B14 · ruled by: cfd-owner-20260923 · at: 2026-09-23T15:16:28Z

### Ruling 14 — Narrow verification-gate repair and one measured retry

Approve only the stopped first-core verification-gate repair within leased tools/verify-application-core.py, then one measured build retry after fresh Coordinator/worker preflight. Root concurs. Owner inspected the old gate, retained process-73926 receipt (exit1,4.381522083 seconds), absent PID and seven untracked files, all within Ruling13 leases. The old tempfile.gettempdir root differed from the assigned path and its fixed name could collide across invocations; the Windows escalation also called unavailable POSIX killpg. Preserve the initial receipt and old task-named scratch for review. Do not broadly clean temp directories, certificate stores or global caches. Before launching any child, allocate a unique per-invocation directory under explicitly selected POSIX /tmp on this assigned host, with exclusive creation; record requested and canonical resolved path (including legitimate /tmp to /private/tmp alias), ownership and invocation ID. On Windows use a separately declared platform-appropriate temporary root; never pretend /tmp is portable or claim Windows execution from source inspection. Point DOTNET_CLI_HOME, NuGet package/HTTP caches, all temp variables and build outputs to that invocation and verify the exact environment before launch. Set DOTNET_GENERATE_ASPNET_CERTIFICATE=false before every dotnet invocation; Microsoft .NET10 guidance documents this first-run prevention at https://learn.microsoft.com/en-us/aspnet/core/security/enforcing-ssl?view=aspnetcore-10.0 . DOTNET_SKIP_FIRST_TIME_EXPERIENCE alone is not that control. Keep telemetry/build-server controls. Do not change HOME or run certificate clean, trust, import or removal. The banner does not prove trust or a new global certificate: Coordinator reports an older existing PFX and no established new global artifact; retain that uncertainty rather than claiming a complete no-side-effect audit. Repair cleanup with platform-correct owned-process termination, including a Windows-safe escalation instead of killpg, and preserve process identity, exit, duration and absence readback. No automatic descendant cleanup or Windows lifecycle guarantee is inferred without execution; unresolved live owned processes stop the route. Fresh cwd/branch/HEAD/leased-path/process/cache readback is mandatory before the single retry. Coordinator checks its actual resolved paths, outputs and final child state before ordinary Ruling13 red/green work resumes within the existing budget. Expected compilation failure from missing APIs is only compiler RED, not behavioral regression RED; meaningful failing assertions remain required. Any repeated path drift, unexpected authorization denial, certificate-generation activity or unmanaged process stops again with evidence; no alternate-tool bypass or mode retry. This repair does not broaden product leases, clear independent integration gates or change the first-core model-evidence exception.

- request: req-01M37E2ZFFXJ7XE28JNWCN4X75 · ruled by: cfd-owner-20260923 · at: 2026-09-23T15:29:29Z

### Ruling 15 — Dependency-aware numeric overflow diagnostic ordering

Resolve the FoilDSL sections3/7 ambiguity as follows. Lexical spelling and encoding errors are classified immediately from bounded tokens and precede syntactic errors. A well-spelled decimal is not classified as binary64 overflow from its unscaled token. Preserve its exact rational value until the grammar establishes its quantity role and actual unit, then perform exact scaling and one ties-to-even conversion. Thus 1e309 with mm can be finite while the same decimal with m overflows; an early raw-token overflow check is forbidden. Post-scale overflow retains stable DSL-LEX and Lexical diagnostic category, but this numeric range check is necessarily deferred until its interpretation dependencies are available. Ordered phases order established failures; they do not authorize inventing an earlier-phase failure from an unknown scale. If a blocking syntax error prevents trustworthy binding, report DSL-SYNTAX without speculative overflow for that quantity. The current single-primary-diagnostic parser may stop at that blocking syntax error; this ruling does not require a new recovering parser. Once quantities are reliably bound, detect known post-scale overflow before unrelated lower-priority structural/reference/geometry diagnostics, with deterministic source order among independently established errors at the same priority. Do not use an unknown unit, unknown channel/quantity role or failed binding as an implicit zero decimal scale. Record that binding as unavailable and report its actual unit/syntax/reference diagnostic as applicable; an unavailable dependency cannot be replaced with a convenient dimensionless interpretation. The same dependency principle applies to unsupported interpretation rather than guessing a version or evaluator contract. Malformed spelling, known dimensional overflow and resource refusal remain distinct: Ruling9 DSL-LIMIT is unsupported resource admission, not invented invalid grammar. Source bytes and last accepted geometry remain unchanged on every refusal. Required countercases include malformed spelling plus syntax failure, missing quantity/unit syntax plus a huge well-spelled decimal, syntactically bound 1e309 mm versus 1e309 m, known overflow plus lower-priority bad degree/reference, and unknown unit or channel with a large decimal showing no guessed-scale overflow. Assert stable code/category/span and no follow-on acceptance. Record this as an explicit dependency-aware interpretation of section7 in the owned core proof/tests; root owns any later normative prose reconciliation, not the core author. This changes diagnostic timing only and waives no full-grammar, finite-after-scaling, geometry, identity or product acceptance obligation. The worker may continue independent work while implementing these exact diagnostic cases; no broader route or scope change is made.

- request: req-01M37FY7E4TNV5GTT7GPM344BC · ruled by: cfd-owner-20260923 · at: 2026-09-23T16:02:13Z

### Ruling 16 — Immutable authored projection before adapter freeze

Approve option A as completion of the existing Ruling13 end-to-end M1 boundary, within its exact B leases and serial continuation. Add a public read-only authored-control projection through existing Contracts/FoilSource/AuthoringSession paths and corresponding existing tests; no extra path, parser authority, editor mode or adapter dispatch is authorized. Bind each projection to exact source SHA and parsed/accepted/draft state; accepted projections include accepted revision, Design/Surface identity and evaluator, while draft results identify base/draft/generation and cannot be confused with accepted geometry. Expose ordered leading/trailing authored CV identities, normalized authored abscissae, exact SI ordinates, declared source/display-unit metadata and applicable lock/constraint state needed to identify an editable target. Preserve custom curve-local IDs verbatim; missing-ID candidates are explicitly unaccepted until the existing diff/accept flow completes. Do not expose mutable Definition or owned arrays/collections. Authored assignments expose their normalized and physical span positions and profile identity/name; keep these typed authored facts distinct from derived inspection slices, which add no source revision or control. Provide a public accepted-geometry query/certificate path bound to the same snapshot, and detailed stable diagnostics with source spans/recovery information reachable through the session contract, including source/draft-generation binding on failed assessments. These are presentation/read paths into Core authority, not new writable geometry state. Geometry certification still requires the complete reviewed admission proof; projection availability or static parse success cannot imply certification. A public consumer fixture must open a source with non-default custom CV IDs, labels and authored assignments, discover targets without parsing bytes or guessing cv-N, inspect accepted geometry, begin the correct numeric rail draft, and preserve draft ownership through unrelated inspection. Mutating every outward nested collection or replacing DTO fields must not change session source/history/target; old snapshots remain bound to their old revision. Cover invalid/incomplete draft diagnostics and absent/unavailable geometry without fabricated values. Root/Owner review the actual compiled public consumer seam before C freeze or dispatch. Authorized persistence work may continue independently within the same serial B worker. This closes an identified API gap only; it does not accept the incomplete a8a6351 checkpoint, resolve the separate twist identity/evaluator question, clear scientific/native-store/Windows gates or select a C harness.

- request: req-01M37JXXGRQPXGR13S4WPFVK6X · ruled by: cfd-owner-20260923 · at: 2026-09-23T16:53:36Z

### Ruling 17 — Twist identity preserves degree-curve meaning under evaluator version 2

DECISION: choose option A. Preserve FoilDSL section 6: invert channel abscissa, evaluate the binary64-defined degree spline as a mathematical function, multiply that value by the exact binary64 constant 0.017453292519943295, then round once to binary64 ties-to-even. Do not pre-round each degree CV to radians, and do not introduce an intermediate rounding of the evaluated degree value. For the new evaluator cfdw-cv/2, canonical twist Curve ordinates are the parsed binary64 DEGREE values, with negative zero normalized. Other canonical fields, RFC 8785, BLAKE3 and metre/dimensionless conventions remain; existing literal object keys and foildsl-geometry-4.0 discriminator remain, with evaluator version 2 providing the explicit semantic/identity boundary. Profile evaluator records also consistently pin /2 for this current contract. Canonical inputs are defining records, not tolerance-quantized shape equivalence. Equal canonical inputs therefore retain the evaluator inputs; distinct records may still describe equivalent shapes. This removes the verified CV-rounding collapse without changing the degree evaluation formula. No tolerance waiver is allowed. EVIDENCE: root frozen a8a6351 probe /tmp/cfd-twist-review.pRn02w/results.json and exact-oracle.json; 1.791 versus 1.7910000000000001 degree CV2 gives equal rounded CV radians but angle bits 3f8801bd477ae01e versus 3f8801bd477ae01f at eta .203125 after inverse x, local u .5 and weight 3/8. Strict sine monotonicity proves different exact placed Z; overlapping returned intervals alone are not the proof. COMPATIBILITY: source grammar remains 4.0 but accepted new sources explicitly declare evaluator version 2. Never reinterpret /1 as /2, silently rehash saved rows, rewrite historical source, or transfer certificates/run freshness. An unavailable /1 evaluator yields a stable version/evaluator refusal and no session adoption, preserving original bytes; any future migration must explicitly create new source/history and retain originals. No automatic migration is authorized. All source/certificate/assessment/session/design/native/telemetry and cache bindings must use the same explicit /2 contract; stale /1 bindings are refused. AUTHORING SCOPE: existing B leases cover production changes and regressions. Root owns coordinated normative/document changes: FoilDSL sections 1/6/8 and conformance cases, valid foil/section examples, current illustrative mockup evaluator claims, design/ADR/proof and B0 fixture algorithms/expectations, generated HTML/index and graph flags. Preserve immutable supplied/v3 references; label any retained /1 artifact as legacy or refusal evidence. Coordinator must inventory and assign any companion path beyond B before writes. REQUIRED PROOF: exact retained collision pair now has distinct /2 Surface hashes while degree-derived angle oracles and coordinate enclosures remain correct; old /1 source/native/assessment refusal leaves accepted state unchanged; /2 save/reopen/Undo/Redo preserves source and matching identities; comment/name/ID/lock-only and equivalent-unit/format changes retain intended identity invariance. Sweep hardcoded evaluator strings and identity goldens rather than changing only the hasher. Root independently reviews the normative reconciliation and unchanged/final numerical oracles; Owner does not self-clear scientific or identity veto. C freeze and geometry acceptance remain held until that review; persistence and R16 work may continue.

- request: req-01M37K8WVY8PYRS4MQXSCGS5ZV · ruled by: cfd-owner-20260923 · at: 2026-09-23T17:08:24Z

### Ruling 18 — Certification includes deterministic query feasibility

DECISION: choose option A for the M1 Certified admission used by Apply and accepted inspection. Besides continuous topology and enclosure error, admission must establish deterministic arithmetic and algorithmic feasibility for PointAt and SectionAt over their entire supported query domain: every finite binary64 eta and x in [0,1], both profile sides and both span halves where applicable, under the pinned evaluator and declared arithmetic/depth/iteration/operation caps. A width bound or a successful finite sample grid is not a completion proof. The frozen constant 1e-300 degree source is a verified counterexample: Certified followed by arithmetic-budget GEOMETRY-NOT-ASSESSED. AUTHOR CHOICE WITHIN B: use a computable conservative arithmetic envelope before issuing a certificate, or a proved bounded analytic reduction such as a tiny-angle remainder enclosure, or their combination. No new dependency, unchecked larger cap, omission of outward rounding, or replacement of angle by zero without propagated error is authorized. Bound intermediates as actually constructed, including pre-reduction rational products/sums, inverse subdivision, normalization, trig and final coordinate conversion; finite input magnitudes alone do not bound those intermediates sufficiently. A small-angle branch must prove its whole branch domain, transition and error propagation to final coordinates; fixing the single example does not prove all admitted queries. If feasibility cannot be proved within this subset, Assess returns explicit NotAssessed before a certificate or Apply authority exists, retaining source and prior accepted state. This is resource-limited implementation admission, not invalid FoilDSL grammar or invalid geometry. OPERATIONAL LIMIT: no hard wall-clock or scheduler guarantee is inferred. Cancellation, process/environmental resource failure and a declared elapsed-time deadline remain distinct observable query outcomes that retain accepted state; they must not be presented as new evidence that the certified geometry is mathematically invalid. Existing one-second cooperative checks must be described as such, not as a universal completion promise. Deterministic cap exhaustion on an otherwise admitted valid query is a certificate/implementation defect, not an accepted ordinary query-specific NotAssessed escape. EVIDENCE: retained frozen root results /tmp/cfd-twist-review.pRn02w, plus arithmetic source inspection. REQUIRED PROOF: executable constant tiny positive/negative twist, zero, subnormal-angle and branch-boundary cases; adversarial inverse/normalization and arithmetic-cap boundary cases; exact or independent high-precision enclosures for successful queries; pre-admission refusal and unchanged session state where the conservative envelope fails. Tests validate the proof implementation but do not substitute for its all-domain argument. Publish the computable bound or analytic derivation and exact supported domain in proof/design; carry it in the authority-bound certificate rather than an unbound UI promise. Root independently attacks the final proof and regression results. This ruling authorizes repair in existing B Geometry/Contracts/session/tests/proof leases only, does not clear the scientific veto or C gate, and does not block independent persistence/R16 work.

- request: req-01M37K97D5AB76155J9ZPN71MA · ruled by: cfd-owner-20260923 · at: 2026-09-23T17:08:48Z

### Ruling 19 — Directory-relative overwrite exclusion and session-bound I/O telemetry

DECISION: approve the bounded repair in existing B leases. The verified Mac case-alias counterexample shows that claims derived from raw target spelling do not serialize writers to the same entry. Use one fixed reserved directory-relative cooperative overwrite claim, created exclusively through the already-held parent directory handle. All cooperating overwrites in that actual directory contend on that claim, even for different targets; this conservative false contention is an explicit M1 tradeoff, not a filesystem case-normalization guess. Equivalent parent path spellings must reach the same directory claim. Reserve internal claim/temp names from user targets, retain symlink/ancestor/regular-file and held-entry identity checks, and revalidate disk content/entry immediately before publication. Atomic no-replace create retains its separate primitive. This claim does not exclude noncooperating processes or make check-then-rename an OS compare-and-swap; retain the declared cooperative-overwrite capability boundary. Existing/stale/unowned claim or temp entries fail closed; never delete them merely by name. Cleanup must prove ownership, and successful durability must cover final directory state after owned temp/claim cleanup. Preserve publication-known versus durability-confirmed outcomes across cancellation and cleanup/flush errors. TELEMETRY: ProjectStore(session) emits measured actual read/save/publication/flush outcome and latency into that SAME session-owned 256-event ring, using a narrow trusted persistence hook rather than arbitrary public event injection. Bind trace/operation correlation deliberately across the asynchronous boundary; record known bytes, stable result/error codes and publication/durability facts without path, source, profile/user name or raw exception contents. Session capture/ack time is not native I/O time. For standalone store use, a private owned session/ring is permitted and disposed by the store; the app must inject its existing session and must not retain a second hidden ring. Disposing an injected store does not dispose the caller session. Define async lifecycle explicitly: no new operations after disposal, no retained events after owned-ring disposal, and telemetry/session disposal failure must not turn a known publication into an untrue not-saved result or mask its durability state. Do not retain source data merely for instrumentation. REQUIRED PROOF: same-file case aliases cannot both own claims on the measured Mac volume; different names in one directory show the declared contention; parent aliases and claim/temp target-name rejection; unowned collision preservation; cancellation/failure cleanup and post-cleanup directory durability; measured normal and refused/failed/cancelled I/O events with exact ring retention/privacy/correlation; injected-versus-owned disposal and in-flight outcome behavior. Native Windows claims remain unverified until actual platform evidence; no portable enforcement claim follows from Mac tests. Root independently reviews the repair and proof; this is design authorization, not B acceptance or a route extension. No additional authored B paths are granted.

- request: req-01M37KQC83J9MSEJ2AGYKGYMAJ · ruled by: cfd-owner-20260923 · at: 2026-09-23T17:09:20Z

### Ruling 20 — Independent evaluator version 2 companion handoff accepted

DISPOSITION: accept bounded companion commit bc0f46d on feature/application-foundation, based on 6e855a4, for Coordinator-controlled handoff into the clean isolated B tree after its current store/R16 checkpoint. No blocking finding in the reviewed normative/data/Test/UX compatibility seam. Owner independently inspected the 30-path companion packet and committed diff plus derived index; directly observed clean root tree. FoilDSL sections 1/6/8 and DSL-19/20 preserve mathematical degree-curve meaning, retain degree CV identity under explicit /2, and name /1 refusal without migration/adoption. Current foil/section fixtures and illustrative v7 emitter/parser agree. Historical /1 receipts and original references remain labeled historical. Architecture/design/ADR state deterministic all-query feasibility separately from cooperative elapsed-time limits and preserve the directory-claim cooperative boundary, owned cleanup/durability and one-ring actual I/O instrumentation contract. EVIDENCE: committed B0 Program SHA256 158297562c3a50db26cdd944c2c63290e9516b88666c043f25561f594d51effa matched the retained green source byte-for-byte. Inspected retained RED collision assertion failure, actual recount JSON of 93 C# checks plus 42 Python checks and 2505 vectors, and process receipt exit 0 in 4.597695167 seconds with recorded quiescence. Inspected new four named assertions and strengthened recount requirements; these are fixture identity/session checks, not production geometry proof. Verified current mockup and harness hashes against proof; inspected 14-check/15-cell browser receipt and final rendered screenshot, which remain prototype evidence. Independently executed python3 tools/check-docs.py: exit 0, 101 artifacts, zero problems, 73 nonblocking review-suggested flags. No duplicate build/browser execution is claimed. LIMITS: this accepts implementation of the companion decisions, not the scientific sufficiency of the eventual R18 algorithm, production /2 binding/refusal, filesystem publication, telemetry privacy/lifecycle, native UI/accessibility, Windows or M1. Root retains independent scientific/store veto; B and C remain held until their required final gates. Coordinator performs normal register union/derived graph regeneration and join checks; production author receives the joined exact revision before dedicated R17/R18 edits. No additional production path lease or routine C route is granted.

- request: req-01M37MVG36GPRHF5JP7NKX22VV · ruled by: cfd-owner-20260923 · at: 2026-09-23T17:27:58Z

### Ruling 21 — Conditional serial Sol route for the first native adapter

DECISION: choose option A for one first C desktop-plus-CLI adapter track, conditional on the gates below. Use a fresh built-in Codex collaborator requested as gpt-6-sol for routine implementation of the frozen contracts. Record the actual spawn request and returned worker identity. If the host does not expose effective inference model identity, record Effective model: Not recorded; this is an explicit narrow C exception, not a claim that the effective model is Sol. Do not silently substitute another requested model if selection or launch is rejected: return the concrete failure to Coordinator/Owner. Owner/root retain difficult architectural, numerical and compatibility decisions and independent review; the C author does not invent a second parser, identity/evaluator, certificate authority or persistence policy. This is an explicit successor-track decision, not an automatic extension of the first-core Astra exception. USER PREFERENCE AND PARITY: Grok/Agy remain the preferred routine routes when qualified. Current Agy evidence includes an unexpected authorized write denial and alternate-tool write; current Grok ACP evidence includes a failed protected-sentinel check plus unverified loaded cwd and cancellation, and selected/effective alias limitations. Those profiles remain unqualified under R6. No new Grok/Agy or model-probe loop is authorized. Codex receives the SAME observed-only standard: no filesystem-sandbox, automatic child cancellation, exact effective-model or lower-cost claim. Actual comparative coding cost is Not recorded. The positive differentiator is existing built-in cwd/HEAD readback and an observed interruption plus explicit owned-child cleanup procedure, not brand or a claim of stronger confinement. The existing first-core cancellation drill is reusable evidence for this unchanged built-in harness control path; it does not establish model identity or guarantee automatic process propagation. LAUNCH GATES: C remains undispatched until complete B has clean committed handback, independent scientific/Data/Test/Security acceptance including R16-R19, successful conductor join, and an independently reviewed compiling consumer freeze against the actual final public API. In particular, freeze a bounded viewport projection using one current assessment, authored custom-ID controls/stations, accepted/draft/recovery bindings and truthful publication/durability acknowledgement. The 24 paths in contract-c-native.md at a494d9c are provisional candidates only. Coordinator then assigns exact live leases, a fresh session/branch/worktree at the joined base, no open decision line, one worker/no subagents, and the initial at-most-90-call/55-minute/100k-context checkpoint budget. Native/Avalonia API spikes, dependencies or extra paths still require their applicable bounded contract/ownership process; this ruling does not grant them implicitly. BEFORE WRITES: read back real cwd, branch, exact HEAD, clean status, session identity, requested/effective model fields, exact path lease and absolute unique task-cache roots; run current coordination doctor/leader/cap/claim checks. Keep at most three active seats as already agreed. Launch no child without PID plus start-identity capture and task-local .NET/NuGet/temp settings including disabled certificate generation. LIFECYCLE: on stop/interruption cease dispatch, identify descendants, terminate only verified owned live processes and read back quiescence; the existing drill proved that interrupt_agent alone leaves children alive. Unexpected denial, identity/path/cache drift or unowned live processes stops the route for a typed decision, not an alternate-tool replay. No new qualification loop follows automatically. HANDOFF: retain source fingerprints, actual native/CLI/gate receipts and unrun platform limits; compare the complete owned-path diff and precommit cwd/HEAD/session binding, then independent root review and conductor join. The author can prepare a UI rubric but cannot clear its own UX/accessibility or other hard veto. Mac live interaction/AX and Windows cross-build/runtime are separately labeled evidence. A bounded checkpoint or passing adapter tests is not M1 acceptance. No C dispatch, production proof acceptance, path expansion, remote push/deployment or cost-saving claim is granted by this conditional route alone.

- request: req-01M37NS4W345FRFM14Z4F0XNSP · ruled by: cfd-owner-20260923 · at: 2026-09-23T17:45:48Z

### Ruling 22 — Bounded production core and macOS store gate accepted

DISPOSITION: accept bounded production B at clean cce9ee52c2966ecbe9c866b31e946aef80d55072 for Coordinator-controlled conductor integration, with independent root review 575f2a1. This is the internal B gate for the documented conservative subset and macOS persistence capability, not M1 or full-language/scientific-analysis acceptance. Owner directly observed clean handback and inspected production session/native replay, source-owned immutable authored projections, public certificate consumers, /2 identity/version guards, directory-relative cooperative exclusion, owned cleanup, disposal and single-ring redacted I/O telemetry. No Data/Test/Security/API blocker was found in this bounded review. Direct source SHA256 binding: Geometry 59aef82a565822c39166cd630091a506eb8e21c86183d5da1185d4a2211a8f9e; AuthoringSession 2d95f0139cd807cc739815ca474ff8f9cd95930281eb086b17ec9693e6e8b405; ProjectStore 613d31240b55dadba2a04c7e0017024542349e60b52d4473ebb8fd8c34254ee0; Contracts 32dccc65bf06185e20faf8089e14471ca1a7b62981c36342d3b6a8a922b00eac. SCIENTIFIC INDEPENDENCE: root, not Owner or author, clears actual-production R17/R18 veto for frozen Core DLL 86a9e74ba2c4054840df81fa5d7438ec50306d5fd21805674b43f0dcb5eaa261. Root inspected the all-domain common-denominator/inverse-depth/intermediate/conversion/Taylor-grid/placement derivation and ran an independent 204-assertion consumer plus exact Fraction enclosure oracle. The collision pair now has distinct hashes; tiny-angle queries enclose the specified meaning; deterministic resource admission is separated from elapsed-budget/cancel outcomes. Samples do not establish universal certification; the reviewed derivation does. Root also independently cleared R16/R19 composition with 23 macOS store/projection assertions and retained separate 120-command replay evidence; unchanged Store/Contracts fingerprints bind that earlier seam evidence to this handback. Final authentic /1 native refusal now returns DOC-VERSION before adoption, preserving input/session state. EVIDENCE: Owner read author proof, root committed review, exact oracle result and retained integrated receipt /tmp/cfd-application-core-20260923-integrated-1b_1cn36: exit0,11 gates passed,6.687411792 seconds,live empty/quiescent. Nested frozen9ty5gkbm has172 author tests; root204 is separate independent evidence. candidate-results.json records196 before eight final native/Example/history assertions; the harness and final stdout account for204, so the earlier snapshot is not relabeled. No repeated fullbuild was necessary or claimed by Owner. HANDOFF: Coordinator reconciles exact leased-path inventory, official metadata/lesson controls, joins core and independent review with the supported conductor, and performs required integrated recounts. Before join root labels or corrects the stale final review paragraph that says a final verdict is pending; this is documentation consistency only, not a production re-test. C remains held until successful B integration and independently reviewed compiling final consumer/API freeze with one-assessment bounded geometry projection, followed by R21 worker/lease/cache/lifecycle preflight. No C dispatch is performed here. LIMITS: Windows runtime and native store remain unsupported/unverified; macOS live UI/AX, native dialogs, packaging/distribution, adapter budgets, full language/profile/tip geometry, tessellation/export and simulation remain outside this B acceptance. No noncooperating-writer exclusion, hardware power-loss or hard-real-time guarantee is inferred. Existing scientific outcomes mean geometric admission only, never hydrodynamic validity. No remote push/deployment or new production lease is granted.

- request: req-01M37PTDT12CX2P8FT2FKJ644M · ruled by: cfd-owner-20260923 · at: 2026-09-23T18:01:50Z

### Ruling 23 — One contained C build-output correction after path drift

DECISION: choose A, conditional on the provenance preflight below, for the SAME stopped C worker/session/model request and existing authored leases. No new model/tool fallback, qualification loop, broader product scope or production acceptance. R21 correctly stopped when default MSBuild outputs appeared outside the declared scratch output root even though they remained inside the assigned isolated worktree. Owner directly observed untracked Core/Persistence bin and obj plus adapter/test directories, and five project.assets.json files naming task-local NuGet packages but source-tree obj output paths. Direct ps for18055/18251/18491 returned no processes. Existing accepted tools/verify-application-core.py uses one dotnet build with --artifacts-path, --disable-build-servers and UseSharedCompilation=false, then executes the built DLL: reuse that observed per-project output convention rather than inventing shared BaseIntermediateOutputPath values or relying on dotnet run defaults. PROVENANCE BEFORE RETRY: Coordinator records exact assigned cwd/branch/HEAD/leases and initial clean readback; inventories each task-created generated directory with canonical path, ownership, size/hash metadata and no symlink traversal; preserves all first RED/GREEN/compiler outputs and commands. Original command/environment/PID/start/exit observations currently live in tool transcript, not retained raw process logs: label any materialized receipt transcribed, and mark missing fields Not recorded. Do not manufacture a raw receipt or effective-model claim. Coordinator supplied read-only global metadata observations for the18:19-18:21UTC window; inspected top-level NuGet/.NET directories predate it and keychain mtime18:18:28Z predates first observed child18:19:33Z. Those limited locations and directory timestamps are not exhaustive proof of no cache/certificate-store effect; no baseline or certificate contents were read. Preserve that uncertainty. There is currently no evidence of trust change. No global certificate/trust/cache cleanup or permission override is authorized; any positively identified unexpected global/trust change stops the correction for review. NARROW ARTIFACT HANDLING: preserve original outputs by default. If relocating outputs exposes old generated C# to SDK input globs, either explicitly exclude those exact generated dirs from the corrected build or quarantine only provenance-proven task-created bin/obj pairs for Core, Persistence, CLI, CLI.Tests and Desktop.Tests into retained task scratch, preserving an exact manifest and relative paths. Do not delete them, traverse links, alter tracked B source, touch unrelated untracked files, use git clean, or clean the worktree broadly. An uncertain ownership case stays untouched and is reported. CORRECTED RUN: use a fresh unique invocation scratch with explicit canonical readback, distinct project/configuration output/intermediate paths under its artifacts root, and all six DOTNET_CLI_HOME/NUGET_PACKAGES/NUGET_HTTP_CACHE_PATH/TMPDIR/TMP/TEMP roots task-local. Record certificate-generation=false, disabled build servers/shared compilation and telemetry opt-out before child launch. Build the relevant project/solution once with the observed --artifacts-path mechanism; do not introduce colliding flat intermediate paths. Retain exact argv/environment, stdout/stderr, status, PID/start identities and descendant quiescence. Afterward inspect actual resolved assets/output paths and the source-tree diff/manifest; an empty requested out directory is not evidence of containment. A successful build may run the already-built targeted test DLL without an implicit rebuild, using the same retained controls. EXIT: this authorizes one measured correction attempt, at most10minutes, followed by Coordinator receipt inspection. Any repeated unexpected output/cache/path effect, unexpected authorized denial, unmanaged child or identity drift stops again; no blind second correction. If corrected containment is demonstrated, normal same-scope R21 implementation/TDD may resume without another routing ruling; an expected compiler or behavioral RED is recorded as that outcome, not relabeled as a containment or product PASS. Newly reported CLI evaluator-exit and section-unit defects remain ordinary same-scope implementation/review obligations, not waived by this permission. Complete C/native UI/accessibility/Windows/M1 gates remain open; no join, push or deployment is authorized here.

- request: req-01M37R2NXSX4C8Z6V9TP3BEBPD · ruled by: cfd-owner-20260923 · at: 2026-09-23T18:25:15Z

### Ruling 24 — Bounded Avalonia telemetry opt-out and collector lifecycle correction

DECISION: choose A for one corrected build in the same C worker/session and existing leases, using the documented process-local AVALONIA_TELEMETRY_OPTOUT=1, with exact owned-process containment retained. This is a supported telemetry opt-out, not a verified generic no-daemon MSBuild property. Owner read installed Avalonia.BuildServices11.3.2 README and targets, the raw failed-build receipt, and directly observed PIDs22486/22494 absent. Root and Owner read pinned task source at https://raw.githubusercontent.com/AvaloniaUI/Avalonia.BuildServices/777f975b0a0cecf0311273711d56697212c558c0/BuildTask/AvaloniaStatsTask.cs : environment opt-out returns before collection in the default path; Community/Trial deliberately override it; POSIX collector uses Process.Start without waiting. Therefore --disable-build-servers alone does not suppress this package child, and README/source inspection alone does not prove the actual retry has no collector. BEFORE RUN: preserve /private/tmp/cfd-adapters-desktop-build.mwdf8T and its raw receipt, stdout/stderr,114 artifact files and cleanup observations. The missing-partial-class compile failure is compiler RED, not a behavioral test. Retain the exact collector command, task-local package path, PID/start identity, original parent/process-group relationship and exact TERM/absence readback. Where pinned package metadata makes the first build-time telemetry storage location determinable, record bounded read-only location/mtime facts without identifier, license or credential values; do not claim absence of prior emission or global writes. Initial telemetry transmission is Not recorded. No global telemetry/cache/trust cleanup is authorized. CORRECTION: apply the bounded missing-partial repair inside the existing C lease, then run one measured build with AVALONIA_TELEMETRY_OPTOUT=1 present in the actual child environment before launch, all R23 scratch/output/cache controls, and retained PID/start/descendant receipts. Do not suppress CoreCompile, misstate DesignTimeBuild, clear UsedAvaloniaProducts, edit cached package targets, strip/change licenses or otherwise bypass a license condition. Do not log license item values. Observe any package warning that opt-out is overridden. The unchanged requested Sol/effective-Not-recorded model contract remains. LIFECYCLE: track descendants while the build is live, retaining identities before reparenting; parent exit is not quiescence. If the exact task-owned collector is observed, it may be terminated only after rechecking PID plus start identity, known ancestry/process group and the task-local pinned collector path; then read back absence. A reused PID, different executable/path, unproven ownership or another unmanaged process stops without broad kill. No process-name pkill or global dotnet shutdown is permitted. If opt-out still starts a collector or reports a license override, this attempt ends after safe owned cleanup and Coordinator review; do not silently admit persistent per-build collection/cleanup or repeat modes. A later decision must account for the actual package/license behavior. EXIT: one attempt within10minutes, then Coordinator inspects exact environment, build result, process observations and output-path manifest. A successful supported opt-out with observed quiescence allows ordinary same-scope C implementation/TDD to resume under R21/R23; keep the environment setting and lifecycle checks in subsequent gate/package commands. A successful build is not native UI, AX, Windows or M1 acceptance. No new dependency/model probe, path expansion, production join, remote publication or claim of zero prior telemetry follows from this ruling.

- request: req-01M37S2ZQHERGYS6SQ4EAEDG1H · ruled by: cfd-owner-20260923 · at: 2026-09-23T18:42:50Z

### Ruling 25 — Native adapter candidate retained; C integration and M1 remain blocked

DECISION: choose A. Retain clean isolated C candidate de105f025eedd0f2df8acc2fbeb16c22750a6ec6 as a source-bound review candidate; do not join its production adapter branch or label C/native M1 accepted. The existing no-partial-join rule is not amended. Accepted joined B remains accepted within R22 limits. Owner directly observed the clean candidate HEAD, read its proof and independent root review at documentation-only join44710f7, inspected the exact final verifier receipt and independently compared all52 recorded input hashes to the candidate with zero mismatches. Receipt SHA2569888b5ce35882930e450974341d9d06069b193b0a781a15061968ce439368349 reports11 zero-exit commands, unchanged source inputs and generated outputs, zero artifact symlinks and empty remaining process groups. These support bounded build/test/package/startup facts; they do not establish rendered or accessible application behavior. Root separately cleared named CLI/controller/recovery defects on identified frozen checkpoints and retained final source-binding evidence. BLOCKING EVIDENCE: root independent native review still withholds its UX/accessibility veto. The supported CUA exact-path bind returned cgWindowNotFound -10005 for the uniquely identified review app, yielding no handle, screenshot, native AX tree or keyboard interaction. Window.Opened/startup logs and the real apphost smoke cannot substitute: smoke intentionally skips Example loading. Final recompilation binaries differ from the earlier live review-copy package even though product source/tests match; no final-binary native claim is inferred. Windows runtime/store, native interaction budgets, rendered hard states, accessibility, dialogs and window/theme behavior remain unassessed as named in the proof/review. No screenshot or AX artifact is fabricated to satisfy a file inventory. NEXT ELIGIBLE ACTION: await a concrete external change in window visibility or supported native-tool access, using the already pending visibility question rather than another launch/profile loop. After that change, freeze and record the exact review instance/source/binary manifest and perform one fresh supported native inspection; then complete the root review checklist for rendered Example/edit/save/recovery flows, keyboard/dialog/focus, real accessibility tree, minimum-window overflow/theme/hard states and measured interaction budgets. A visible window or single screenshot alone does not clear every outstanding row. Any resulting implementation fix remains within an assigned lease and requires appropriately bound regression evidence. Root independently clears native veto only from actual evidence, then Owner reviews a new gate request and Coordinator performs the supported join. No new launch variant, alternative UI control bypass, harness/model qualification loop, broad process cleanup or remote publication is authorized now. DOCUMENTATION AND HANDOFF: Coordinator may join status/proof/ruling documents through normal register ownership, preserving the isolated production branch, runnable development packages and retained receipts. Report completed B and source-bound C build/package work separately from blocked native acceptance; do not call the user objective or Windows/macOS M1 complete. Preserve exact-owned process lifecycle and leave unrelated user processes alone. Signing, notarization and distribution trust remain distinct release obligations, never inferred from a portable package. This disposition is a concrete evidence gate, not a rejection of the useful candidate or authorization to discard its work.

- request: req-01M37ZMZQF20AXC198X5EHG38Z · ruled by: cfd-owner-20260923 · at: 2026-09-23T20:36:54Z

### Ruling 26 — Reopen macOS creation-mode boundary with a serial ABI repair

DECISION: authorize one separate serial B persistence repair track; reopen R22 only at the macOS file-creation permission/ABI trust boundary. C production integration and M1 remain held until this repair and the other independent native gates pass. The user-cleared window visibility blocker does not waive storage security. Owner directly read the production fixed-signature Open/OpenAt and OwnedEntry.Create path, the controlled ctypes probe and its retained receipt SHA256 474341b5ea211509650b24c8419998839654a8ca1383a4bf15617a7a277a587f, and directly read back the original accepted.cfdw.json mode0454,7836bytes,SHA256 b5fa3b7cdc6360ce559881caa443b4f44e3db3bb4c9d38ecff8d6f902c5ce150. Two fixed-four trials gave0000 and two three-fixed-plus-variadic trials gave0600 under0022 umask. This supports the native calling-convention mechanism; it is not a measured managed repair. Existing science/session acceptance is not withdrawn, but historical store PASS is insufficient for this newly exposed boundary.

REPAIR DIRECTION AND SPIKE: use a minimal fixed-signature native C bridge compiled against the actual macOS system headers, with the C compiler making the genuine variadic open/openat calls. Keep the public managed persistence contract unchanged. Before adopting it, the assigned author must inspect the installed compiler/header/runtime/publish contracts and run a bounded real .NET10 arm64 create probe through the proposed bridge, including errno propagation and packaged-library loading. A native bridge is conditional on that executable spike and independent review, not accepted by prose. Do not emulate the arm64 varargs layout with guessed dummy arguments, depend on an undocumented private runtime entrypoint, or retain the wrong fixed-four declaration and mask its result with fchmod/chmod. A post-create mode correction cannot prove secure mode at creation and is not the selected repair. No application-wide umask change. Review both current variadic imports as the same defect class; preserve held-directory and no-follow semantics, no pathname-only replacement. A failed spike stops with evidence and a typed alternative; it does not authorize a dependency/probe loop.

ROUTING AND PATHS: Coordinator creates or assigns a clean isolated repair tree at the current integrated B revision, records exact base/session/branch, and dispatches one requested gpt-6-astra built-in author for this native trust boundary. Effective model stays Not recorded if unexposed; reuse the observed-only identity/path/cache/lifecycle standard and explicit owned-child cleanup, with no new harness qualification or enforced-confinement claim. This is a difficult native ABI repair exception, not an expansion of routine C routing. C author retains its current UI lease and cannot edit B. Wait for C author clean checkpoint/yield before the serial repair starts; no overlapping production author. Coordinator may freeze only the necessary subset of these seven exact repair paths: src/CfdWorkbench.Persistence/ProjectStore.cs; src/CfdWorkbench.Persistence/CfdWorkbench.Persistence.csproj; src/CfdWorkbench.Persistence/native/cfd_store.c; tests/CfdWorkbench.Core.Tests/ProjectStoreTests.cs; tests/CfdWorkbench.Core.Tests/CfdWorkbench.Core.Tests.csproj; tools/verify-application-core.py; docs/proof/application-core.md. Any different/additional authored path needs an explicit seam; this does not lease root review or shared lesson/security registers. Coordinator owns normal register/graph integration. Packaging adapter paths remain C-owned and receive the reviewed B change through the normal clean handoff. First checkpoint is at most45calls/25minutes for red, ABI/loading spike and proposed patch; cap means report/replan, not automatic extension. Keep the existing task-local caches, certificate-generation false, Avalonia opt-out where applicable, no build servers, PID/start tracking and quiescence controls.

PROOF FLOOR: retain a real production-path RED reproducing the forbidden creation mode, with independent OS stat rather than only the same managed Stat decoder. In isolated child processes exercise declared umasks0000,0022,0077; expected mode is requested0600 filtered by that child mask, with no execute/group/other/special bits. Inspect newly created temporary and overwrite-claim descriptors before the first content write and final published inode after create-only and overwrite. Include a pre-write fail-closed mode/type check through the held descriptor; if that check fails, no project bytes are written or published and owned-only cleanup preserves pre-existing/collided entries. Preserve the known-result save/flush/uncertain telemetry and acknowledgement contracts. Test no-clobber create, cooperative overwrite/case-alias exclusion, partial-write/cancel/error cleanup, replaced-name ownership, missing/unloadable native helper, errno mapping, Unicode path handling and exact source/history roundtrip. Existing atomic link/rename, ancestor checks and no-follow protections may not be weakened to obtain correct permissions. Build and publish loading must be exercised from the actual output/package directory, with source/helper/binary fingerprints and declared target architecture; cross-compilation alone is not runtime proof. Windows remains fail-closed/unsupported for the native store; never introduce a macOS helper load on Windows or claim a new platform capability. Unix mode proof does not establish an untested ACL or distribution-signing guarantee.

INDEPENDENCE AND HANDOFF: retain the original anomalous saved file and all probe receipts unchanged; no automatic chmod, migration, broad cleanup, unrelated file inspection or global permission/trust changes. Root independently reviews Security/Data/Test code and real filesystem evidence and repeats a source-bound packaged native Save/Reopen with observed mode and byte/history preservation. Author and Owner do not clear their own trust-boundary work. Update the durable defect-class control through its designated owner and require the new permission oracle in the normal verification gate so this class cannot hide behind successful byte roundtrips again. After clean bounded handback, independent review and a fresh Owner gate, Coordinator joins the B repair, hands it into clean C, and recounts affected integrated/package/native evidence. This ruling authorizes repair work only: no production C join, M1 acceptance, remote publication, broad platform promise or waiver of remaining UX/accessibility/Windows gates.

- request: req-01M381H81G425H2Y4JH874GBQG · ruled by: cfd-owner-20260923 · at: 2026-09-23T21:10:18Z

### Ruling 27 — Review-only B and C composition for packaged native proof

DECISION: choose A. Permit one isolated review-only composition of clean B permission-repair ee6d73ad3abebb7e10930190e4ad9be7f747a556 and clean C adapter candidate 6f168c44a560b574980b56a0fc3c889c91a2acc2, solely to obtain the packaged native evidence required by R26 and the outstanding C review. This is a verification workspace, not a canonical product join, partial acceptance or amendment of the no-partial-join rule. Owner directly observed both assigned worktrees clean at those exact HEADs and read root's committed investigation checkpoint: independent managed create/overwrite/bytes/history cases under umasks0000/0022/0077 passed with OS-stat0600; owner-read-stripping0400 refused without publication/residue. That evidence does not replace actual combined application Save/Reopen.

COMPOSITION: Coordinator owns the temporary assembly and creates a fresh, named isolated review worktree/branch through the supported coordination workflow, recording the current joined base and both complete candidate ancestries. Include the complete required C candidate, not merely its final three-file repair commit. Preserve original B/C branches, clean trees and receipts unchanged. Assemble exact candidate production and test blobs through an explicit owned-path inventory; use normal register union/derived regeneration for metadata only. A semantic source conflict, missing prerequisite, unexpected path or source change stops for the relevant owner, not an ad-hoc Coordinator repair. Retain a manifest mapping every compiled source/project/gate/test input to its originating candidate or joined-base blob and the combined tree/HEAD. No new production source edits are authorized in the review tree. Temporary does not mean auto-delete: retain tree, package and receipts as review evidence until the normal unheld/clean/no-unique-commit cleanup conditions and explicit removal authorization are satisfied.

BUILD AND BIND: run one contained full combined verification/package sequence with the already accepted unique task-local caches/output paths, disabled certificate generation/build servers/shared compilation, process-local Avalonia telemetry opt-out and exact owned PID/start/descendant tracking. Preserve stdout/stderr/exit/timing and actual quiescence. Verify hashes of source before and after, resolved outputs, packaged Persistence DLL and the bridge, including library architecture and actual package-relative loader location. Do not silently substitute the old C package or copy an unbound helper into it. A gate failure remains an isolated review result; do not merge to fix it or repeat a profile/launch loop. The packaging sequence may produce its normal macOS/Windows artifacts, but cross-publish is not Windows runtime/storage proof. Record any review-only bundle-identity adjustment separately with exact changed fields and byte manifest; it cannot modify executable code, persistence configuration or accepted user data.

NATIVE REVIEW: root performs the supported CUA review against the exact identified combined app instance and package manifest. Use fresh task-owned files in a recorded private test directory. Exercise actual native Save, accepted/draft or recovery semantics as applicable, Save/Reopen source/history/identity preservation and independent OS mode readback; test the actual repaired package rather than only the public-store consumer. Preserve the original anomalous accepted.cfdw.json and its hash/mode unchanged. Pending focus, numeric-edit, keyboard, recovery and other C checklist rows can be reviewed in the same source-bound instance; they keep their individual evidence/dispositions. No alternative UI-control bypass, chmod of evidence, broad cleanup, unmanaged child, remote publication or model/harness qualification is authorized.

EXIT AND INDEPENDENCE: root independently clears or retains Security/Data/Test and UX/accessibility vetoes from actual code and executable evidence. Neither author, Coordinator nor Owner may replace that review with this staging permission. Return combined input/output/package/launch manifest, exact native results and remaining limitations; then request the R26 B gate and C disposition explicitly. Only subsequent eligible Owner disposition plus supported conductor integration may move canonical product branches, followed by required affected recounts. No B permission repair, C product or M1 acceptance is granted here; Windows runtime and release/distribution trust remain separate. Coordinator may proceed with this bounded review staging after control/leader/lease renewal, without a new author or model launch.

- request: req-01M383WPHECQWJ4M4H181CKW78 · ruled by: cfd-owner-20260923 · at: 2026-09-23T21:50:22Z

### Ruling 28 — Bounded macOS creation-permission repair accepted for B join

DISPOSITION: choose A. Accept the bounded R26 macOS arm64 creation-permission repair at clean B ee6d73ad3abebb7e10930190e4ad9be7f747a556, with independent root review 17f60d0f80918557ce9950ea18a9faa005fbdbae, for supported local B conductor integration only. The reopened R22 permission/ABI boundary is closed for this measured platform and scope. This is a completed B correction, not a partial C join or M1 acceptance.

INDEPENDENT GATE: root, not Owner or the author, explicitly grants Security/Data/Test PASS from source inspection, original managed production RED, strict prewrite guard/mutation controls, independent packaged public-store create/overwrite/reopen under umasks0000/0022/0077 and the actual R27 combined app-bundle native Save/overwrite/Reopen/Undo/Redo. Source/history/accepted identities remain exact. Owner directly observed the clean B and root review HEADs, read the candidate C bridge/managed loading and failure/ownership diff plus author proof and root final review, inspected the disk oracle, and read back modes0600 for both saved results and unchanged mode0454/hash for the original anomaly. No repeat native test or full build by Owner is claimed.

BINDING: reviewed combined HEAD e6e562843b000e27542efa1ee7dc2bb251eb2b4f; manifest SHA2564fa156928a6bd0ae8b0d44fc97099488c7337b27c90e1827a18cfe306e9b3799. Owner independently compared all55 manifest input blobs to both their declared originating revisions and the combined commit: zero mismatches. Retained gate receipt SHA2566b8a4f748f0211fb34e1677e7ed8bf9221a7bbbd1397e9806151548d6d31cf44 records11 zero-exit steps, unchanged source inputs/outputs, empty retained process groups and no observed collector. Owner directly checked the review app's Persistence DLL35b38f817c739efd5ccc669316acee6234040ece5e66755fb405b3d18365b3be and helper90181814d8acebf1b322579d0ff41964f63311e946b34c3df1c04698e9b24c88 against the root binding. Disk receipt /tmp/cfd-native-review.czEz9w/combined-native-save-receipt.json SHA256c3cd500d11fd1bf062833c8c71e048d3a9c0173a25bb9c31d10ab0ce4b65b75c records exact sole trailing-control source edit, unchanged leading/other bytes, preserved accepted records and appended cursor history. Root's real CUA observations supply the interaction provenance; disk JSON alone does not.

ACCEPTED POLICY: both variadic OS entrypoints are called by fixed-signature C exports compiled against system headers, with assembly-local managed imports. New held regular-file descriptors must have exactly0600 before project bytes can be written. Zero mode, extra permission/special bits and owner-bit loss refuse and attempt only proven-owned cleanup; an owner-stripping umask is unsupported rather than repaired. This stricter owner-read/write policy is explicitly accepted within the R26 fail-closed direction. Missing/unloadable helper and unmeasured architecture remain unsupported; no chmod mask, process-wide umask change, path-only fallback or weakened cooperative atomicity is accepted. The normal core gate now exercises real independent mode observations, including prewrite claim/temp and final create/overwrite, so byte roundtrips alone cannot clear this defect class.

JOIN AND HANDOFF: Coordinator renews current leader/clean/path/lease controls and integrates only the six authored B repair paths plus designated metadata and independent review documents through the supported conductor. Do not import the review-only combined C branch as the B join shortcut. Perform the required affected core/integrated recount with retained controls; a failed join gate stops completion. Preserve original B/C branches, anomalous file, review package and receipts. After successful B join, hand the reviewed B revision into clean isolated C through the normal controlled handoff. Existing source-bound C observations remain evidence at their recorded revision; new changes or binary differences require the applicable binding/review, never automatic acceptance. Shared investigation/lesson/status records must distinguish historical failure from current bounded repair and keep their executable-control links; no ad-hoc register repair or new source lease is granted.

LIMITS: C production remains isolated pending its separate native/UX/performance disposition. This ruling does not accept full M1, Windows runtime/store, Intel-macOS store, ACL inheritance, signing/notarization, release trust, noncooperating-writer exclusion or unmeasured filesystem power-loss durability. The review notes a macOS helper in Windows publish inventory; it is not evidence of Windows loading because the runtime guard fails closed, and target-specific release packaging remains an explicit residual. No remote push, deployment, evidence permission change, automatic migration, broad cleanup or model/qualification loop is authorized. Root's independent veto clearance is specific to this B repair; remaining scientific/native/platform limits retain their prior status.

- request: req-01M38GAB4D08SN8SWB4QW3YZF2 · ruled by: cfd-owner-20260923 · at: 2026-09-24T01:28:31Z

### Ruling 29 — Bounded C portability repair and truthful native timing proof

DECISION: choose A for one bounded continuation of the existing requested-Sol C author under R21's unchanged observed-only route; effective model remains Not recorded if unexposed. R28 B-only acceptance/join remains intact. C is still an isolated candidate, not product-integrated or M1 accepted. Owner read the typed request, current C contract sections3-4 and root native review, directly observed clean handoff13c883ad4951bca4ca24d50a8201159e0f2a9588 and inspected the named Python text-I/O seams and current Preview/Refresh event path. The reported downstream conductor failure at step8 is an incomplete handoff verification, not a successful11-gate run; preserve that RED and the successful canonical B join a550466 separately.

SCOPE/SEQUENCE: Coordinator renews leadership, exact source/cwd/HEAD/lease/cache controls and returns the existing author to the clean C handoff containing accepted B. One author/no subagents, maximum three active seats. First repair only the five named C-tooling findings in tools/package-application.py and tools/verify-application-adapters.py: two stdout encoding guards, explicit newline on the identified write_text, and explicit encodings on two text-mode check_output calls. Use the installed repository control's accepted portable shape; preserve gate logic, exits, process ownership and receipts. Run the two failed controls as targeted RED-to-GREEN evidence before adding runtime measurement. No suppression, allowlist relaxation or full-suite replay merely to obtain another exit code. After targeted success, implement and prove truthful normal-path timing in the necessary subset of existing C-owned paths: src/CfdWorkbench.Desktop/Program.cs, App.axaml.cs, MainWindow.axaml.cs, Viewport.cs, WorkbenchController.cs; tests/CfdWorkbench.Desktop.Tests/WorkbenchTests.cs; docs/proof/application-adapters.md; and the two named tools. Coordinator freezes the actual subset explicitly. This is no lease for B source, normative thresholds, selected design, root's independent review or a new tooling dependency.

MEASUREMENT CONTRACT: inspect and spike the installed Avalonia rendering/lifecycle API before depending on it. Start timing at the actual native input event or documented process-launch boundary, using a monotonic clock with stated units. Bind completion to the corresponding operation/generation and the final intended state: edit feedback rendered in the relevant inspector/status/canvas; Preview completed with its current result rendered; Cancel accepted state restored and stale background completion suppressed. The first Notify/Refresh displaying Assessing is not Preview completion. Dispatcher scheduling, handler return, layout completion, render-command submission and actual frame presentation are distinct; explicitly identify which supported signal is observed. A framework signal that only proves submission cannot silently become end-to-visible evidence. If presentation cannot be measured faithfully with available supported APIs, retain the best correctly labeled measurement and report the remaining endpoint gap as Not assessed; do not manufacture a PASS from a proxy. Root's native observation independently validates state/content correlation. CUA tool-call elapsed time is not application latency.

Emit redacted normal-path operation/status/duration/volume facts; no user source/path/names/vertex values/hash or new exporter/MRU. Keep in-flight measurement state bounded and drop stale/cancelled correlations truthfully. Reuse existing event conventions and a bounded transient measurement lifecycle; no second durable telemetry store or silent B public API extension. If the existing C-only boundary cannot provide required measurement without altering the accepted session telemetry contract, send a typed seam before that change. Use a controlled known-delay negative control and stale-generation/cancel overlap cases to prove the timer includes final work and does not close at the interim Assessing frame. Keep measurement overhead visible.

BENCHMARK/REVIEW: retain raw per-operation measurements, sample count, percentile definition, failures/cancellations, cold versus warm status, fixture/source hash, actual machine/runtime, package/input hashes and endpoint uncertainty. Use at least30 measured edit, Preview and Cancel trials per declared representative admitted fixture after separately reported warmup; at least3 fresh-process actual Example-ready launch trials with no startup-smoke bypass or claim that OS disk caches were flushed. Show p95 and maximum; the existing 100ms p95 edit,250ms p95 Preview,250ms Cancel acknowledgement and5s cold-launch obligations are not weakened. Existing sparse15-query M1 Example measurements are not the full normative21-station/201-slice/50k-triangle/10k-plot reference workload; report that coverage limit explicitly and do not invent unsupported mesh/analysis work to simulate compliance. Preserve one-current-assessment and bounded-query correctness. A missed threshold is a measured finding; investigate from the trace and propose a bounded correction, not automatic broad optimization, fewer correctness checks or a changed budget.

For native state coverage, author prepares a source-bound finite matrix using the existing review harness; root performs independent supported CUA checks. Include all current light/dark/high-contrast themes, reduced-motion modes, minimum1024x700 and normal window, keyboard/dense authored-control navigation, admitted accepted/draft/Preview/Cancel, invalid/recovery, pending missing-ID import, error/Not-assessed and unavailable-analysis states. Use explicit representative combinations that cover each state/theme and the risky dense/minimum-window intersections; no unsupported claim of a full Cartesian matrix. Densest admitted authored lists retain every item and custom ID with scrolling/selection/AX correctness; an unavailable normative-density fixture remains a named gap. Preserve exact native Save/Reopen and original anomaly evidence. Do not bypass CUA with external UI automation; screenshots/headless tests alone cannot clear keyboard/AX/performance.

BOUNDS/EXIT: first checkpoint at most60calls/35minutes for the portability correction, rendering-contract spike and measurement/control implementation. Root's independent native review is a separate active-seat handoff, not concurrent author self-review. At the bound, return completed evidence, named gaps and a concrete continuation estimate; do not silently extend. Expected behavioral RED while implementing its named control is valid TDD; an unexpected gate failure, path/model/cache drift, unmanaged child or denial stops for Coordinator review. No model/harness probe loop. Retain existing unique artifacts/caches, certificate-generation false, Avalonia opt-out, build-server controls and PID/start/descendant quiescence. At clean final candidate, perform the required affected tests and one eligible integrated gate/package sequence with exact manifests, then independent root review and a new C disposition request. This authorizes work and measurement only: no C product join, threshold waiver, Windows runtime/store, ACL/signing/distribution or M1 acceptance. Root retains every independent veto.

- request: req-01M38GZW3WYW5M2SDSDQVEX3JX · ruled by: cfd-owner-20260923 · at: 2026-09-24T01:40:15Z

### Ruling 30 — Repair theme shadowing with runtime-resolved resource controls

DECISION: choose A. Authorize one bounded repair by the existing C author, under unchanged R21/R29 identity, observed-only containment and independent-review controls, in exactly four paths: src/CfdWorkbench.Desktop/Styles.axaml; tools/verify-application-adapters.py; tests/CfdWorkbench.Desktop.Tests/WorkbenchTests.cs; docs/proof/application-adapters.md. Coordinator renews the exact leases against clean C3a1d67bcb8ec4bc38a2b8adb8d885b10e1619cc3. No B source, global Escape routing, new dependency/model profile, normative threshold, unrelated interaction or C product-join change is authorized.

EVIDENCE/CAUSE: Owner directly observed clean C HEAD, read its real Styles.axaml, verifier and tests, root's native dark review and typed high-contrast report, and pinned Avalonia11.3.14 ResourceDictionary.TryGetResource source at https://raw.githubusercontent.com/AvaloniaUI/Avalonia/11.3.14/src/Avalonia.Base/Controls/ResourceDictionary.cs. Root dictionary lookup precedes themed lookup. Fourteen root brush definitions therefore shadow the nine Dark and fourteen HighContrast definitions. The verifier's base_brushes|overrides models the opposite precedence. The prior static contrast PASS is not runtime theme proof. Root independently observed unreadable mixed dark controls and a light/teal high-contrast instance and retains the UX/accessibility veto; Owner does not claim a new native run or precise screenshot-derived contrast ratio.

REPAIR: move the light brush definitions into the Light theme dictionary, provide every required custom brush in Dark and the existing HighContrast variant, and remove conflicting root brush definitions. Shared immutable color/spacing/type tokens may stay where they do not shadow theme-selected brush keys. Preserve DESIGN token values and the selected interface; reuse existing viewport colors explicitly where intentional. Confirm default/system Light/Dark resolution and the existing custom HighContrast variant, without claiming operating-system high-contrast integration from a review selector. If a modal or another source-owned component requires a behavioral change outside these four files, report the specific seam before editing it.

RED-FIRST CONTROL: load the actual application XAML resources through the installed Avalonia runtime and query them using the actual Light, Dark and custom HighContrast theme keys. First capture the old candidate failing expected resolved-resource assertions. Do not construct an independent hand-written test palette, parse XML and union dictionaries, or only assert that theme declarations exist. Record each required resolved brush/color and missing/incorrect-resource failures. Include a controlled root-key shadow mutation or equivalent preserved failing fixture so the test detects this defect class after the fix; no mutation of final production artifacts. A theme-correct palette still does not prove a control uses it: inspect representative applied foreground/background values from actual styled controls, covering toolbar/button and tab text, normal/read-only/error text fields, panels, selected/focused controls and viewport/annotation colors. Retain actual theme/selector and any inherited framework color in the receipt. Tests that cannot instantiate a relevant control must state that gap and leave it to the required native review, not substitute token arithmetic.

VERIFIER: the normal adapter gate must execute/consume the source-bound loaded-resource results and calculate contrast from resolved colors for the critical rendered pairs, including framework/custom combinations implicated by the observed mixed palette. Preserve the existing critical4.5:1 threshold and applicable UI token/accessibility obligations; do not lower them to make a theme pass. Fail on absent/empty theme results, missing required keys, stale source/binary binding, unsupported brush values or a copied static report. Static no-shadow/completeness checks may supplement but cannot replace runtime resolution. The proof records old false-PASS mechanism, executed RED and GREEN, negative control, exact source/package binding and untested visual states. Coordinator captures the generalized test-oracle/runtime-precedence defect and executable control link in the designated shared register.

EXECUTION/HANDOFF: initial checkpoint at most45calls/30minutes for scoped resource/test/gate correction; no automatic extension. Expected named RED during TDD is evidence; a new boundary/path/cache/identity failure stops for Coordinator review. Keep task-local cache/artifact roots, certificate-generation false, Avalonia telemetry opt-out, disabled build servers and exact owned child lifecycle/quiescence. After targeted controls pass, run one changed-source full adapter gate and package, preserving logs, input/output fingerprints and actual process results. A full-gate failure stays a failure; no waiver or blind repeat. Hand back a clean four-path source change plus official metadata exceptions through their owners.

Root then independently reviews source/tests and the exact rebuilt native dark and high-contrast packages via supported CUA, checks representative empty/accepted/draft/error/recovery and modal/selection/focus readability, and verifies Light did not regress. Existing named timing outcomes remain bound to their original binary; no submission/visible threshold redefinition or new performance PASS is inferred from this repair. Source-bound package/launch parity must connect new native observations to the new gate. Author cannot clear its own UX/Test veto, and this Owner repair ruling is not veto clearance. C/M1 remains blocked by the theme defect until independently cleared and still requires its remaining separate disposition; Windows runtime, full assistive-technology and release trust limits remain. No production merge, remote publication, evidence deletion or new UI automation route follows.

- request: req-01M39WEPK6GNSKCJP036D2PH5R · ruled by: cfd-owner-20260923 · at: 2026-09-24T14:19:18Z

### Ruling 31 — Bounded applied-contrast continuation and conditional app-modal theme repair

DECISION: authorize one same-author continuation of at most35calls/25minutes. Choose option B only with the conditional fifth-path restriction below; the applied-color proof remains the primary task and all R30 thresholds and independent vetoes remain. Current isolated checkpoint has only Styles.axaml, WorkbenchTests.cs and verify-application-adapters.py dirty; Owner directly inspected that inventory, loaded-resource/contrast code and the real unsaved-dialog creation code. No full gate/package/commit or applied-template PASS has been established by this checkpoint. Preserve existing42-resource GREEN and root-shadow negative evidence, without promoting its15 token-pair minimum6.15 to control contrast.

APPLIED-COLOR PROOF: continue in the four R30 paths by loading real application resources and actual templates, resolving the text/presenter foreground actually drawn and the background behind it through the visual/template ancestry. A control-level Foreground/Background property alone can be a template input rather than the drawn brush. Account for brush alpha, control/ancestor opacity and actual layer ordering over a known opaque backing; white over #33ffffff cannot be evaluated without that backing. Do not treat a translucent color as opaque, use a guessed white/black background, double-apply ancestor opacity, or silently substitute a token pair. Where gradients, overlays, effects or unresolved template content prevent a defensible calculation, record Not assessed with the exact missing fact; no invented ratio or applied-control PASS. Include independent arithmetic controls for opaque and translucent composition and an intentionally low-contrast case to prove the oracle refuses a defect. Keep R30 normal text and critical-pair4.5:1 unchanged; any applicable non-text3:1 checks are additional classifications, not permission to relabel normal text or lower an existing critical-pair threshold. Missing/empty/stale results and failed controls fail closed.

COVERAGE: Light, Dark and actual custom HighContrast must cover the relevant rendered control states, selected/focused tabs/list items, enabled/read-only/error numeric/source content, toolbar and unsaved modal; disabled samples must be explicitly identified rather than confused with active editing. Root still independently verifies native readability/focus after the changed-source package. Resource-only checks remain useful but insufficient. The verifier must report unresolved applied rows honestly and must not describe a resource-only result as R30 theme acceptance.

MODAL CLASSIFICATION AND CONDITIONAL PATH: the unsaved prompt in MainWindow.axaml.cs is an app-owned Avalonia Window constructed by UnsavedDialogAsync, not an operating-system file picker. It is part of the app theme contract: an explicitly selected Dark or HighContrast owner should not silently open an app-owned Light prompt. An OS-native file picker remains a separate platform observation and is not restyled by this ruling. Root observed the app-owned prompt Light under Dark, and Owner confirmed its current initializer sets no theme. First reproduce through the real modal creation path and record main/dialog ActualThemeVariant plus resolved/applied colors. IF the app-owned prompt resolves a different variant from the explicit owner selection, Coordinator may activate exactly one additional lease, src/CfdWorkbench.Desktop/MainWindow.axaml.cs, solely to propagate the active owner theme to this unsaved prompt by the supported framework mechanism. Test the inherited Light/Dark/HighContrast result, default/system behavior, safe Cancel focus and unchanged Save/Discard/Cancel outcome before and after the minimal correction. No generic dialog rewrite, global Escape routing, owner/input routing change, new theme selector or unrelated MainWindow edit. If the real probe does not reproduce the mismatch, do not edit the fifth path; report the discrepancy to root. This condition resolves the app-owned theme seam now without author self-authorizing a wider path.

CONTROL/HANDOFF: Coordinator renews leases and requested/effective model, clean-base-plus-declared-diff identity, task cache/output and PID/start/lifecycle controls; the existing dirty checkpoint is retained, not reset. No new author/model qualification or dependency loop. Tests and production correction are scoped to the reported defect; a newly exposed out-of-scope seam is reported before editing. At the bound return exact completed claims, unresolved rows and receipts; the cap does not authorize another extension. After targeted controls genuinely pass, perform the single changed-source full gate/package already authorized by R30, with source/binary/helper and process receipts, then clean handback and root independent native Dark/HighContrast plus Light regression review. A missing applied oracle remains a named blocker even if other tests exit zero. Preserve root-owned review artifacts and shared-register ownership. No B/persistence change, performance endpoint/threshold waiver, global Escape repair, C production join, M1/platform/assistive-technology/release acceptance or remote publication is granted.

- request: req-01M39XGJPGMB307FK5Y60K3F69 · ruled by: cfd-owner-20260923 · at: 2026-09-24T14:37:35Z

### Ruling 32 — One coherent five-path applied-contrast completion packet

DECISION AND REQUEST CORRECTION: choose A as one coherent same-author completion packet, with exactly FIVE already authorized paths, not the request draft's same-four-path wording: src/CfdWorkbench.Desktop/Styles.axaml; src/CfdWorkbench.Desktop/MainWindow.axaml.cs; tests/CfdWorkbench.Desktop.Tests/WorkbenchTests.cs; tools/verify-application-adapters.py; docs/proof/application-adapters.md. R31's conditional modal lease has been activated; Owner inspected the actual one-line owner-theme propagation and current four-source-file dirty inventory. MainWindow remains restricted to that modal theme seam. Replace the draft45calls/30minutes with one bounded70calls/50minutes completion window, including proof and eligible gate/package. No new path, model route, B edit, global Escape or performance-threshold change. Preserve dirty checkpoint and retained receipts; Coordinator renews leases and identity/cache/lifecycle controls before resuming the existing author.

WHY REPLAN: the measured progress is loaded-XAML42/root-shadow controls and modal owner-theme/safe-Cancel checks, with specific HighContrast property defects corrected. These do not constitute the required applied-control contrast oracle. Repeated property-only probes were an incorrect work-unit granularity; further isolated property printouts are not the completion strategy. Coordinator captures this class with a control that requires the entire declared theme/control/state row set before the gate can pass. This explicit replan changes implementation granularity; it does not treat a fired cap as evidence of completion.

COHERENT PACKET AND LOOP VARIANT: before the next targeted execution, declare and implement the finite required set of (theme,control,state) rows, the real-template extractor, actual painted-background evidence, supported contrast calculation, strict receipt parser, negative controls and gate wiring together. Use one real MainWindow lifecycle per Light/Dark/HighContrast to exercise the required states sequentially; retain a Default/system modal inheritance check separately. The remaining unproved required rows are the loop variant, visible in each result. Do not derive the expected set solely from emitted rows or shrink it when a row is difficult. Include enabled toolbar/body text; selected/unselected tab text; enabled/focused numeric text; the active Source tab with real nonempty read-only text; an actual selected Example station/control item; error/status text; viewport annotation text; unsaved modal text/buttons and focus indication. These are representative cases already required by R30/R31, not an exhaustive new control library. Record actual enabled/read-only/focused/selected/theme state so a disabled placeholder cannot satisfy an enabled row. Disabled/inactive controls may carry an explicit standards-applicable exemption but cannot replace a required active sample.

SMALLEST SUFFICIENT ORACLE: a general alpha compositor is NOT required if the real required rows can be proved by a conservative opaque method. It is permissible to capture the actual template text/presenter foreground and painted ancestry, require relevant control/group opacities to equal1, ignore fully transparent paints, and use the nearest actually painted opaque backing at that text location. Verify that selected ancestors/bounds and inspected template painting establish the backing; a conveniently named ancestor or unrelated brush is not evidence. Any contributing partial-alpha brush/group, unresolved overlay, gradient/effect, unsupported paint or missing backing must fail closed with its precise row/reason. Never silently flatten, guess white/black or call such a row assessed. This conservative refusal accounts for unsupported compositing rather than pretending to solve it. Retain raw template/ancestry values, bounds/state and the supported foreground/background pair. Existing opaque and controlled transparent/partial-alpha cases must prove correct acceptance/refusal; an intentionally low-contrast opaque case and missing/duplicate/stale row cases must be refused. R30/R31 text4.5:1 and applicable focus/non-text3:1 classifications remain; no relabeling text or weakening an existing critical-pair threshold.

EXECUTION BOUND: after the coherent implementation, run the targeted complete-matrix control once and inspect every required row and negative control. Permit at most ONE evidence-directed corrective retry of that targeted complete matrix. A compilation-only failure is retained as compiler evidence and does not count as a behavioral run, but it consumes the same total call/time budget and cannot become an unbounded build loop. If a required row remains unsupported or failing after the corrective retry, stop with the smallest concrete blocker: row, actual template/paint/state, missing contract and required remedy. Do not request another generic allowance merely to print more properties, replace the oracle with token arithmetic, or claim full contrast from loaded-resource GREEN. Existing useful resource/modal tests remain in the coherent gate.

FINISH CONDITION: only when the full required set and negative controls genuinely pass, finalize proof and run the single changed-source full gate/package already authorized by R30, with no post-gate source edits hidden by old receipts. Record actual source/input/output/helper/package fingerprints, process ownership and quiescence. A full-gate failure or containment drift stops for Coordinator review, not a blind repeat. The70call/50minute window is a ceiling, not a reason to delay a completed handback or an automatic extension. Use existing task-local cache/output, disabled certificate generation/build servers and Avalonia telemetry opt-out controls. Retain original anomaly/native review files unchanged.

INDEPENDENCE/LIMITS: root independently reviews the actual extractor/parser/controls and rebuilt Dark/HighContrast plus Light native behavior through supported CUA; author and Owner do not clear their own UX/Test veto. Coordinator owns shared defect/register/graph work. No C product join, M1 acceptance, visible-performance waiver, Windows runtime/store, broad assistive-technology verdict, ACL/signing/release claim, new dependency/harness loop or remote publication follows from this work authorization.

- request: req-01M39Y6T9B7E0HK17AK655XJ7Q · ruled by: cfd-owner-20260923 · at: 2026-09-24T14:50:16Z

### Ruling 33 — Correct locked-target fixture under a finite decreasing-blocker matrix

DECISION: choose A as an explicit fixture-recovery exception to R32's exhausted corrective retry, not a silent extension or a product-contrast PASS. Permit the same C author one bounded completion window of at most50calls/35minutes and at mostTHREE complete targeted matrix executions, subject to the decreasing-blocker rule below. The five exact R32 paths and their restrictions remain; no new production handler, B, global Escape, model route or threshold change. Original source checkpoint and failed receipts remain retained.

VERIFIED FIXTURE DEFECT: Owner directly inspected WorkbenchTests.cs selecting ControlList.SelectedIndex=1, the actual Example rail IDs and MainWindow's Editable guard, and the existing tests that already obtain an editable target from the authored projection. Root and author independently identify this target as root_mirror-locked leading cv-1. The missing owned draft is a fixture prerequisite failure; refusing edit is correct product behavior. It does not prove a contrast failure or success for the unexecuted rows. Select a real UI item by the core-owned rail/control identity whose authored Editable is true, rather than replacing index1 with another guessed index. Assert projection identity, UI-selected identity, unlocked/editable status, owned draft rail/vertex/generation, and actual enabled numeric field before numeric contrast collection. Retain a locked-target negative assertion proving refusal; never disable its guard or manufacture a draft. Sweep this matrix's other positional state assumptions before rerun, reusing existing fixture helpers where adequate.

BATCHED COLLECTION AND LOOP VARIANT: freeze the exact72 (theme,control,state) rows already required by the current matrix/parser and a finite list of their declared fixture prerequisites before execution. Define U as every unproved prerequisite plus every required row not yet proved by a valid current-run observation, including failed and Not-assessed rows. Preserve identities and report the whole U set each run, not just a count. Collect all independent prerequisite and row failures across themes in one run. When a prerequisite fails, mark only dependent rows Not assessed with that reason and continue safe independent checks; retain a nonzero overall result. Use fresh isolated state per theme as needed and close owned windows/processes deterministically.

The initial correction run must resolve the observed locked-target prerequisite and expose the remaining full matrix honestly. A further corrective execution is permitted only for specific recorded failures and only while the prior run's U is a proper subset of its predecessor, with no formerly passing required row regressed; the retained stopped run supplies the initial unresolved baseline. All unexecuted rows already belong to U, so discovering their reason must not be counted as successful completion. Identical/increased U, any unowned boundary problem, or the third execution without a full pass ends this packet with the exact remaining blocker. No counter reset, row deletion, replacement by disabled exemptions, threshold change, weaker opacity/paint evidence or pass-marker-only fix. Compiler-only failures consume the same total budget and remain compiler evidence; they do not authorize an unbounded build loop. This finite exception cannot automatically renew itself.

PROOF/EXIT: keep the R30-R32 actual template/painter/opaque-backing and strict parser/negative controls. Use the author budget for the fixture correction, coherent matrix completion, proof and the previously authorized single changed-source full gate/package only if every required row and negative control truly passes. Existing red evidence is not erased. A full gate failure stops for Coordinator review; no C join follows from any green result here. At a stop, report row/prerequisite, observed state/paint, what is missing and the narrow remedy rather than another generic request for more probes. Coordinator captures the generalized positional-fixture-versus-domain-capability failure and its identity/editability assertion as the preventive control through shared-register ownership. Root independently reviews actual oracle code and final native themes; Owner and author do not clear their own UX/Test veto. All task-cache, telemetry opt-out, certificate-generation, exact PID/start/quiescence, evidence preservation and remaining C/M1/platform/release limits remain.

- request: req-01M39Z58ADE5CTBZWFQ99RESE6 · ruled by: cfd-owner-20260923 · at: 2026-09-24T15:06:06Z

### Ruling 34 — Measure tab-focus coordinate failure before one independent-reviewed correction

DECISION: choose A for one narrow diagnostic-to-correction packet, at most40calls/30minutes of author work, within the same five R32/R33 paths and existing actor/containment contract. This is an explicit exception after R33 correctly stopped at an unchanged unresolved set; it does not reset the general matrix retry loop. Preserve the68/72 receipt and earlier failures. No B source, production input handler, global Escape, dependency/model route, contrast threshold or C product-join change.

EVIDENCE STATUS: Owner directly inspected the current dirty inventory and the focus.tab oracle's compound TransformToVisual/TranslatePoint/target-bounds predicate. It throws a combined message without retaining the actual failed operands. Owner read pinned Avalonia11.3.14 AdornerLayer.cs at https://raw.githubusercontent.com/AvaloniaUI/Avalonia/11.3.14/src/Avalonia.Controls/Primitives/AdornerLayer.cs: its composition path associates an AdornedVisual and arranges from target-local bounds. That makes a logical-versus-composition coordinate mismatch a testable hypothesis, not the established cause. The four geometry rows remain Not assessed;68 passed rows cannot clear them.

ONE DIAGNOSTIC: run a focused source-bound tab-focus probe collecting all four affected theme variants, without rerunning the full matrix merely to rediscover the same message. Bind source/test/framework versions and actual target/adorner identity. Emit target, outer and inner ring bounds; every relevant transform matrix and translated origin; local/root coordinate spaces; clip bounds and clip-enable state; render/composition adornment relationship where observable; focus/visibility; brush colors/thickness, group opacity and the backdrop/painter ancestry. Split each currently combined predicate into named operands/results so the precise failure is visible. If a composition fact cannot be accessed through the installed supported/test boundary, say Not recorded rather than inventing it. Internal observation of the test's own application is not authorization for an alternative external native UI automation route.

INDEPENDENT INTERPRETATION BEFORE REPAIR: author stops after the diagnostic and sends the raw receipt and exact source to root. Root independently checks the coordinate/clip interpretation against the pinned implementation and observed runtime relationships, and records whether the defect is in the oracle mapping or actual focus geometry. Coordinator may resume the already authorized single correction only after that explicit root review. No human approval is required and no fresh model probe is authorized. If cause remains unproved, hold with the exact inaccessible fact or contradictory observation; do not assume AdornedVisual alone proves rendered overlap.

ONE CORRECTION: if evidence establishes an oracle-coordinate defect, correct the mapping into the actual painted coordinate space while retaining object identity, clipping, opacity, bounds and true-background checks. If it establishes a product style defect, permit only the corresponding bounded Styles.axaml correction within the existing lease. Do not simply remove a bounds condition, expand tolerances, substitute target dimensions, ignore clipping, or treat an out-of-target ring as if the target were necessarily its backdrop. A ring outside the target requires evidence of its actual underlying paint or a valid style correction. Include a displaced/wrong-target or clipped-ring negative control proving that the corrected oracle still rejects a false mapping. Preserve4.5 text and applicable3.0 focus/non-text thresholds, exact72-row set, and all previous opaque/unsupported-paint and strict receipt controls.

VERIFY/EXIT: after that root-reviewed single correction, execute at mostONE complete72-row targeted matrix. Required outcome is72/72 with valid observations and negative controls, not a changed pass label. Any remaining row, new regression, unsupported mapping or containment failure stops this packet with a precise blocker; no additional corrective matrix is authorized. Only a genuine complete pass permits the previously authorized one changed-source full gate/package and proof close, still within the total envelope or a close-only handback at its cap. Root then independently inspects the exact rebuilt native focus/themes through supported CUA; this diagnostic/oracle review does not self-clear final native UX/Test acceptance. Coordinator retains shared-register ownership and captures the generalized combined-predicate-without-operands failure as an actionable diagnostic control. Preserve source/hash/package/PID-start/quiescence receipts and all original anomaly evidence. No C/M1/platform/release acceptance, threshold waiver, remote publication or automatic further extension is granted.

- request: req-01M3A03ZM0JJKXAN7PQ56J94TA · ruled by: cfd-owner-20260923 · at: 2026-09-24T15:22:59Z

### Ruling 35 — Compile public composition contracts before one unchanged contrast matrix

DECISION: choose A for one compile-time contract correction and one complete72-row matrix, within the same five authorized C paths and existing author route. Initial correction is confined to WorkbenchTests.cs and its proof/gate assertions; no production behavior/style correction is implied by this API-type defect. Bound this close-out at30calls/25minutes including eligible proof and final gate/package. This is an explicit exception to the stopped R34 run, not renewal of an adaptive matrix loop. Preserve the68/72 failure and prior source/receipt fingerprints.

CAUSE AND CONFIDENCE: Owner directly inspected the current RequiredCompositionProperty<T> reflection calls and FocusPlacementFacts using System.Numerics.Vector2/Vector3 for Size/Anchor and Offset/Scale/Center. Root independently reports reflection of the exact installed Avalonia.Base11.3.14 getter contracts: Size and AnchorPoint are Avalonia.Vector; Offset,Scale,CenterPoint are Avalonia.Vector3D; RotationAngle is System.Single; Orientation is System.Numerics.Quaternion. The four rows failed at the type guard before producing placement evidence. This is a missed API-contract check by author and reviewer, not proof of bad focus geometry or permission to weaken its oracle. Owner has not independently rerun that reflection and does not relabel the report as a new measurement.

REPAIR/COMPILE CONTROL: replace reflection for all seven PUBLIC getters with direct strongly typed property access on the installed CompositionVisual API, using explicit declared types so a mismatch is a compiler error. Update the facts/negative fixtures consistently; do not insert broad object conversions or a permissive type fallback that restores the same blind spot. Capture exact referenced Avalonia assembly/package fingerprint and retain a compiling consumer/control against those seven getters before any matrix execution. The wrong-type form must be demonstrably rejected at compile time, using a disposable negative consumer or equivalent retained compiler diagnostic, without mutating final production sources. Inspect any other newly introduced public getter usage in this narrow oracle for the same class.

Retain reflection only for the two existing nonpublic adornment links, AdornedVisual and AdornerIsClipped, with exact member/type checks, assembly/version binding and explicit Not-assessed/refusal on absence or drift. These are test diagnostics, not a production dependency license or a substitute for native observation. Keep all actual target/composition identity, clip, bounds, size/transform, painted-backdrop/opacity and contrast conditions unchanged. Retain wrong-target/displaced/clipped negative controls and raw placement facts. A compile success settles signature compatibility only; it cannot settle compositor placement or contrast.

REVIEW AND SINGLE RUN: send the exact typed-access/fixture diff and compiler evidence to root for independent source review before executing ONE complete unchanged72-row matrix. Coordinator may resume that already authorized run after root explicitly confirms the narrow contract fix; no further Owner or human confirmation is needed for that sequence. Any unresolved row, new geometry contradiction, failed negative control or containment drift stops with precise evidence; no second matrix retry is authorized here. No dropped row, widened bounds tolerance, altered contrast threshold or pass-label substitution. Only genuine72/72 plus existing negative controls permits the previously authorized single changed-source full gate/package and proof close. A failed full gate stops for Coordinator review.

HANDOFF/LIMITS: renew exact leases, source identity, task-local caches and PID/start/quiescence controls; retain original native/project evidence. Coordinator records the missed-public-signature class, shared review miss and preventive compile-time consumer in its owned register. Root independently reviews actual final oracle and rebuilt native focus/themes; neither author nor Owner clears its own UX/Test veto. No B/core edit, new path/model/dependency, global Escape, performance-threshold waiver, C product join, M1/platform/release acceptance or remote publication is granted.

- request: req-01M3A0WKHTGK8N65VSNK890E0D · ruled by: cfd-owner-20260923 · at: 2026-09-24T15:36:27Z

### Ruling 36 — Prove target-bound compositor readiness before the contrast matrix

DECISION: choose A for one focused compositor-readiness spike, independent root review, then at mostone unchanged72-row matrix if readiness is actually established. Same five authorized C paths and author route; at most35calls/25minutes for this packet. This is a narrow test-lifecycle repair after R35's honest stop, not a renewed geometry retry loop or permission to alter product behavior.

GROUNDING: Owner directly read the test's SetupWithoutStarting, synchronous Show/UpdateLayout and subsequent composition-property reads, and the existing production target-bound timing call sites. Owner also read pinned Avalonia11.3.14 CompositingRenderer.cs at https://raw.githubusercontent.com/AvaloniaUI/Avalonia/11.3.14/src/Avalonia.Base/Rendering/Composition/CompositingRenderer.cs: dirty visuals are synchronized inside the queued composition update before commit. Root reports actual zero composition Size in allfour failed tab rows despite nonzero local bounds. Source and observed state support an unproved readiness dependency, not a guarantee that merely adding a delay fixes it.

FOCUSED SPIKE: inspect the installed PUBLIC compositor/dispatcher lifecycle contracts and reuse the established R29 target-bound scheduling pattern where its semantics fit. Compile against the exact pinned types before running. Run the real test application's dispatcher/render loop, retaining identity of the target, adorner, compositor and requested callback/batch or returned completion object. Observe the relevant properties before and after a genuine framework-driven update, including size matching current target bounds, current adornment links, clip and unchanged generation/selection. Use supported scheduling/awaiting; do not set CompositionVisual.Size, synthesize completion, invoke internal SynchronizeCompositionProperties/renderer update methods, or substitute a different detached visual. A nonzero property by itself is not proof of the current target's update.

The readiness barrier must be bounded and target-bound: timeout/cancellation/disposal or stale target/batch must fail closed with explicit diagnostics and owned lifecycle cleanup. Include a deliberate missing-completion/expired-deadline negative proving it cannot silently pass, plus a stale-target negative where feasible within this same barrier. Retain raw before/after/correlation/timing facts. Do not use arbitrary sleeps, unbounded dispatcher pumping, or polling until convenient dimensions appear. If the public API exposes no stable numeric batch ID, record its actual observable object/correlation binding and that limit; do not invent one. Composition synchronization/commit remains separate from on-screen presentation and establishes no visible-performance PASS.

ROOT CHECKPOINT: author sends the focused spike source and receipt to root and stops. Root independently confirms that the barrier waits for the required normal synchronization on this exact target and that the negative refuses; source-only plausibility is insufficient. If readiness remains unknown/zero/stale or times out, stop with that precise blocker and no complete-matrix run. After explicit root clearance of this narrow lifecycle mechanism, Coordinator may resume the already authorized test-only barrier integration and ONE complete72-row matrix without a further Owner/human confirmation.

MATRIX/EXIT: preserve the exact required rows, typed public-property checks, bounds/clips/transform/adornment identities, paint/opacity and contrast thresholds and all prior negative controls. No geometry bypass, bounds substitution, ratio waiver or missing-row exclusion. Any unresolved row or new contradiction after the single matrix stops this packet; no further corrective matrix is granted. Only genuine72/72 and required negatives permit the previously authorized one changed-source full gate/package and proof close within the bound; otherwise return a precise checkpoint. Preserve prior failure receipts and original project/native evidence. Root's final source-bound native UX/Test veto remains separate, and author/Owner cannot self-clear it. Coordinator records missing runtime-lifecycle readiness as the defect class with the bounded barrier/negative as control. Existing task-local cache/artifact, certificate-generation false, telemetry opt-out, owned PID/start/quiescence and shared-register ownership rules remain. No B/core, new path/model/dependency, global Escape, C join, M1/platform/release acceptance or remote publication is authorized.

- request: req-01M3A1ANCKGN0VR4DPY1YJG2NX · ruled by: cfd-owner-20260923 · at: 2026-09-24T15:44:10Z

### Ruling 37 — Reproduce and contain window callbacks after disposal

DECISION: choose A for one focused actual two-window close/queued-refresh reproduction, conditional lifecycle repair and one complete72-row matrix. Bound the same-author packet to40calls/30minutes in the existing five C paths. MainWindow.axaml.cs may expand beyond its prior modal-theme-only seam ONLY for the reproduced post-close callback lifecycle defect described here. No B/core change, process-per-theme masking, theme/performance threshold change or C join.

EVIDENCE STATUS: Owner directly inspected Changed posting Refresh, Closed disposing WorkbenchController, and the later Refresh/read paths. Root reports Light18/18 including synchronized focus placement and DOC-CLOSED in the other themes during the serial loop. Queued callbacks from a disposed prior window are source-supported but not yet established by a measured stack. Capture the actual exception type, stable code, full stack and window/controller/session correlation in the retained test receipt before changing behavior. Do not treat the current message-only catch as sufficient diagnosis or promote Light's18 rows to whole-matrix success.

REPRO: run one focused two-window sequence under the actual dispatcher/render loop. Establish a first window with accepted state, cause a real Changed notification/queued refresh, close that window through the applicable normal lifecycle, then open/use a second window and drain the relevant normal dispatcher work within a bound. Record enqueue/execute/Closed/dispose ordering and identities; preserve raw before-fix failure. Do not close fake detached controls, call Refresh manually after disposal as the sole reproduction, or replace the product queue with a fabricated exception. If the measured stack/identity disproves the proposed path, stop with the actual cause; no speculative lifecycle patch is authorized.

CONDITIONAL REPAIR: after root independently confirms the focused receipt/source establishes a first-window callback after disposal, allow the smallest MainWindow-only lifecycle guard/teardown. Invalidate the window's callback lifetime before disposing its controller on actual Closed, unsubscribe the Changed handler, and ensure already-queued callbacks recheck the original window lifetime when executed; unsubscribing alone cannot remove existing queue entries. Cover directly resumed Refresh continuations in the same lifecycle seam where the reproduction/source demonstrates them. Preserve disposal of the owned controller/store and normal refreshes for live windows. A cancelled unsaved-close attempt is not Closed and must not permanently disable callbacks or the live session. No swallowing DOC-CLOSED globally, catching all errors as success, weakening the core guard, sharing controllers between windows or changing Save/Discard/Cancel semantics.

REGRESSION: retain a deterministic failing-before/passing-after two-window queue test proving no first-window refresh touches the disposed session, the second window still receives real updates, and unsaved-close Cancel leaves the first window functional. Retain the existing core/session DOC-CLOSED refusal as expected behavior after actual disposal. Include a controlled removal/bypass of the new lifecycle guard in a disposable test copy or the retained pre-fix repro as the negative; do not let test isolation itself hide the defect. Root reviews the exact repair and runtime regression evidence before the full matrix continuation.

VERIFY/EXIT: after that independent narrow clearance, Coordinator may resume ONE complete72-row matrix across allfour themes in the SAME process/normal loop. No process-per-theme alternative is authorized to evade this lifecycle issue. Preserve typed/compositor-readiness, geometry/clip/paint/opacity, contrast and required-row/negative controls. Any remaining prerequisite/row failure or new contradiction stops with precise evidence; no further corrective matrix is granted here. Only genuine72/72 permits the previously authorized changed-source full gate/package and proof close within the bound. Final native themes/focus/lifecycle review remains root's independent UX/Test veto and is not cleared by this repair ruling.

CONTROLS/LIMITS: preserve all old receipts, frozen sources and original native/project files. Renew exact leases, model identity limits, task-local output/cache, certificate-generation false, Avalonia telemetry opt-out and owned PID/start/quiescence controls. Diagnostic stacks stay in explicit test receipts; do not add private stack/path data to normal application telemetry. Coordinator captures the generalized queued-callback-after-owned-disposal class with its regression control. No new path/model/dependency, global Escape, B/store/schema/science change, C product integration, M1/platform/release claim or remote publication follows.

- request: req-01M3A29R32WJP5JDFA8JPZ0ZXY · ruled by: cfd-owner-20260923 · at: 2026-09-24T16:00:53Z

### Ruling 38 — Repair measured pointer-state contrast with an expanded actual-control matrix

DECISION: choose A for one bounded same-author theme-state repair/proof packet, at most60calls/40minutes. Coordinator renews exact leases against clean C5bdf0e7edd841405e82cceddf297d1da7e3778d5. Author may edit exactly src/CfdWorkbench.Desktop/Styles.axaml, tests/CfdWorkbench.Desktop.Tests/WorkbenchTests.cs, tools/verify-application-adapters.py and docs/proof/application-adapters.md. Existing MainWindow lifecycle/modal code is read-only for this packet; no new handler, B/geometry/store, global Escape, timing threshold, dependency/model route or C product-join change.

EVIDENCE: Owner directly observed clean C HEAD, inspected its base/selected TabItem rules and existing applied-row inventory, read root's source-bound native HC finding and pinned Avalonia11.3.14 Fluent TabItem.xaml at https://raw.githubusercontent.com/AvaloniaUI/Avalonia/11.3.14/src/Avalonia.Themes.Fluent/Controls/TabItem.xaml. The pinned template directly assigns background and inherited foreground for pointer-over/pressed including selected combinations. Root independently reproduced the disappearing selected FoilDSL label twice and its return after F6 on exact package receipt5de8a214dba8d2e989f57bbf47c9b80b93e2d19ccbe41562e87829dbbf84cf3e. The missing state rules are a supported causal hypothesis; actual painted-state RED must establish the precise assignment before repair. The prior72-row PASS covers its declared states only and is not rescinded or enlarged by implication.

COHERENT STATE SWEEP: first declare a finite table of meaningful enabled control/state combinations from the installed templates. Preserve all existing72 rows and add selected/unselected tab pointer-over and pressed combinations, including return-to-rest and focus-visible coexistence; cover both Section and FoilDSL headers. Sweep the narrow sibling classes already using custom semantic brushes: enabled toolbar/modal buttons and selected/unselected navigator list items for their actual supported pointer-over/pressed states. Do not invent a pressed pseudo-state for a control whose template does not implement one. Record such applicability from source rather than silently omitting a declared row. Use Light,Dark,HighContrast and Default/system variants. Freeze the expanded required row identities before execution; no shrinking on failure or deriving expectations from whatever was emitted.

RED AND REPAIR: obtain actual loaded-control/template text and painter/ancestry evidence for the failing selected HC hover state, including current selected, pointer-over, pressed, focus and enabled flags. Compare entered/released/left state with the same target and package/source binding. Use the installed supported input/test-state mechanism after checking its signatures; never set measured Foreground/Background directly to the expected answer. If only direct pseudo-class activation is available for an automated style probe, label that scope honestly and rely on root's actual native pointer interaction for behavioral proof. Retain the failing pre-fix ratio/paint evidence. Then repair only the demonstrated semantic theme/template resource precedence, preserving selection and focus indicators and meaningful state feedback. Do not hide the header, disable hover/press, erase selection distinction, force one hard-coded palette or replace the control to evade the defect. Apply the same failure-class correction to measured sibling cases within these styles, not unrelated design changes.

CONTROL: reuse the now-proven compositor-readiness, typed geometry, painter/opaque-backdrop and lifecycle controls. Normal text remains4.5:1 and applicable focus/non-text3:1; no state exemption for enabled pointer interaction. Keep disabled rows explicitly classified. The verifier must require every original and new declared row, actual state metadata and source-bound raw resolved paint; wrong-state/default-only/missing/duplicate rows must fail. Retain a negative with the relevant state override removed or the old frozen failing source to prove the new row detects this defect. Batch independent state failures rather than repeatedly probing a property. Permit at mosttwo complete targeted expanded-matrix executions: initial candidate and one evidence-directed correction. A remaining unsupported/failing row, unexpected boundary effect or the call/time bound ends the packet with precise evidence, not a weaker PASS or automatic extension.

FINISH/INDEPENDENCE: after all required rows and negatives genuinely pass, finalize proof and run one changed-source full gate/package with exact input/output/helper/binary and process receipts. Root independently reviews the oracle/style diff and actual rebuilt HC selected-tab hover/press/release, Dark and Light regressions, selection/focus and affected sibling states through supported CUA before any integration request. No native contrast or visible-performance claim follows from automated state activation alone. Preserve all existing review files and original anomaly evidence. Coordinator records the generalized incomplete interaction-state coverage class and the expanded required-row control through its owned register. Existing task-local caches, certificate-generation false, telemetry opt-out, PID/start/quiescence and model-identity limits remain. C retains its hard native veto until independent clearance; no C/M1/Windows/accessibility-completeness/release acceptance, push or distribution is authorized.

- request: req-01M3B7PQT51MYH395S8XR1EZHM · ruled by: cfd-owner-20260923 · at: 2026-09-25T02:55:30Z

### Ruling 39 — Measure the actual TextBox backdrop before repairing native focused contrast

DECISION: choose A for one same-author measurement-first numeric-input paint repair and recurrence-control packet, at most 60 calls/40 minutes. Coordinator renews exact leases against clean isolated C d774e1e. Only src/CfdWorkbench.Desktop/Styles.axaml, tests/CfdWorkbench.Desktop.Tests/WorkbenchTests.cs, tools/verify-application-adapters.py and docs/proof/application-adapters.md may be authored. MainWindow/controller, B/core/store, geometry, dependencies, global Escape, model route and the separate pending M1 scope decision are unchanged. No C join or native acceptance is granted.

EVIDENCE AND CORRECTION: root reports actual fresh R38 High Contrast CUA selected-tab hover fixed twice but an editable leading cv-2 value 5 unreadable white-on-white while focused and white-on-gray after Tab, with AX value/draft identity and accepted identity retained. This is a native failure despite the 312 managed rows. Owner directly inspected clean C source: Backing() follows TextPresenter ancestors, while pinned Avalonia 11.3.14 TextBox.xaml places PART_BorderElement as a sibling behind the text-host Border (lines 104–115) and recolors that painter on hover/focus (199–222). Source: https://raw.githubusercontent.com/AvaloniaUI/Avalonia/11.3.14/src/Avalonia.Themes.Fluent/Controls/TextBox.xaml. The oracle's incomplete paint model is Verified from source; the exact runtime assignments producing the reported white backdrop remain a hypothesis until measured. Existing 312-row output remains execution evidence, not adequate TextBox painted-contrast proof. Do not solve this solely by adding state names.

DIAGNOSTIC GATE: before repairing product styles, reproduce the failing actual loaded MainWindow NumericInput under High Contrast with the same editable authored CV, real owned draft and nonempty value. Capture pointer/keyboard focus origin, IsFocused/focus-within/active-window state, pointer-over, enabled/read-only/error flags, actual theme, TextPresenter text/foreground, caret and selection ranges/brushes, and PART_BorderElement plus overlapping paints with draw order, transformed bounds, clipping and opacity. Compare focused after typing, all-selected and replacement text, pointer exit, and Tab-away/return. Use supported framework input/focus APIs with verified signatures and honest native-versus-managed labels; never force measured colors, fake focus flags, fill a manual composition size or replace real editing with property-only palette checks. Preserve source-bound pre-fix RED and raw facts. If runtime paint coverage cannot be established, stop blocked rather than guessed.

ORACLE: account for the actual sibling backdrop behind the text, not merely its closest ancestor. A conservative template-specific adapter is allowed after verifying exact template identity, sibling order, opaque coverage and no unsupported intervening paint. Unsupported partial alpha, overlay, clipping or ambiguous ordering fails closed; a general compositor is not required. Validate the adapter with a negative that changes the real sibling backdrop while leaving ancestor colors unchanged: the old false-pass shape must now fail. Also retain the old frozen failing theme or remove the eventual repaired state override as a meaningful negative. Do not inject an expected color into the observed control. Independent root review of RED and painter mapping is required before claiming the production fix established.

REPAIR AND SWEEP: repair only the demonstrated semantic resource/template precedence, including foreground/background, selected text, caret or focus border if measurement shows the same defect. Preserve editable value, normal hover/focus/error feedback, text selection, keyboard accessibility and all prior tab/list/button fixes. Sweep the same TextBox failure class across NumericInput and read-only SourceText in Light, Dark, HighContrast and Default. Freeze a finite state table before execution: actual unfocused/rest, pointer-over, keyboard focus without hover, pointer focus with/without hover, nonempty selected versus unselected text, entered replacement value and blur/refocus; classify only truly inapplicable editing states for read-only SourceText. Preserve all original 312 row identities, correcting their painter evidence where necessary. Do not let residual focus silently stand in for a declared unfocused row. Enabled ordinary and selected text must meet 4.5:1 and applicable caret/focus/non-text 3:1; disabled classifications remain explicit. Missing, duplicate, wrong-state, missing-painter and stale-generation observations fail. The declining unresolved set is measured paint prerequisites plus fixed required rows, never a row count adjusted to emitted output.

BOUND AND FINISH: one focused diagnostic RED, then one coherent candidate implementation and one complete targeted matrix; permit at most one evidence-directed corrective matrix rerun. At any unsupported boundary, failed second matrix or budget exhaustion, stop with precise unassessed/failing cases and release leases. Only after the complete required matrix and meaningful negatives pass may the author finalize proof and run one changed-source full gate/package with exact input/output/helper hashes, task-local caches, telemetry opt-out, certificate-generation false and owned PID/start/quiescence receipts. Root independently repeats native High Contrast click/Cmd+A/type5/Tab/return, selection/caret readability and selected-tab hover, then Dark/Light regressions on the rebuilt package. Root alone clears native UX/accessibility veto; the author and Owner do not self-clear it. Coordinator records the failure class as an ancestor-only paint oracle missing overlapping sibling state paints, with the new false-pass negative as its control. No threshold, Windows runtime, visible-performance, release or M1 acceptance follows. Part 2 remains stopped pending the user's separate consolidated scope decision.

- request: req-01M3B9VD4ZP4A68VKWSCBEW5QF · ruled by: cfd-owner-20260923 · at: 2026-09-25T03:33:01Z

### Ruling 40 — Prepare a bounded Windows x64 native-contract spike and qualification workflow

DECISION: choose A. Authorize one bounded W0 contract/executable-spike/CI-preparation packet, at most 70 calls or 45 minutes, whichever comes first. Windows x64 runtime and measured visible timing are approved M1 obligations; full section authoring is M1.1. The earlier pending-scope language is historical. An unavailable interactive desktop does not block this dependency-ready preparation, but W3 native UIA/Narrator and reference-device visible timing remain separate blocking gates.

ROUTE AND OWNERSHIP: Coordinator launches one fresh requested gpt-6-astra built-in author for the difficult Windows Data/Security/process contract seam, under the existing observed-only isolated-worktree route. Record requested model separately from exposed effective identity, using Not recorded if unavailable; no model-identity invention, new Grok/Agy qualification loop or unmeasured cost-saving claim. Enforce the three-seat cap, exact cwd/branch/base readback, leased-path diff and commit identity, task-local outputs/caches, process ownership/cleanup and independent review. The current C author has yielded clean 0d4a590; W0 does not inherit or expand C leases. Freeze exactly six authored files: docs/design/windows-runtime.md; docs/proof/windows-runtime.md; tools/spikes/WindowsRuntime/WindowsRuntime.csproj; tools/spikes/WindowsRuntime/Program.cs; tools/qualify-windows-runtime.py; .github/workflows/application-windows-qualification.yml. Official derived index/audit handling remains with designated register ownership. Existing src/, tests/, accepted verifier/package files, solution/global SDK pin, security registers and C proof are read-only. Any seventh authored file or production API change needs a concrete seam decision first.

EVIDENCE: packet 986049a distinguishes W0/W1/W2/W3. Owner inspected the current IProjectStore/SaveRequest/SaveResult and Supported guard: macOS arm64 only, Windows sharing/reparse semantics explicitly unmeasured, native Darwin binding, Windows core verifier refusal, and Ubuntu-only docs workflow. Scope inventory established Actions-enabled public repo and authenticated repository access but no scheduled Windows job or qualified desktop. GitHub documents an x64 hosted runner candidate; actual image/SDK/capabilities must be recorded by a future job. Do not present the macOS host, PowerShell availability or win-x64 publication as Windows execution.

W0 CONTRACT: compare the smallest viable existing .NET/Win32 handle-based approaches before selecting the spike route; cite current primary API documentation and actual signatures/layout/calling conventions rather than guessing P/Invoke constants or copying the macOS ABI. Map each accepted store invariant to a Windows primitive, failure mode and runnable oracle. Cover atomic create-only and same-directory overwrite, expected-hash conflict, immutable input, cancellation before/after publication, uncertain outcome, identity-bound parent/target/temp/claim cleanup, case/Unicode aliases, alternate data streams/device/UNC or unsupported filesystems, hard links and reparse/junction/symlink ancestors as well as the leaf. A final-component reparse flag or prechecked pathname is not proof of containment across ancestor substitution. Explicitly declare the supported filesystem/path subset and fail closed for unproved cases. Measure exclusive sharing/cooperative writer behavior without claiming exclusion of arbitrary writers. Keep accepted project bytes, /2 identities, history/recovery and stable diagnostics unchanged.

SECURITY AND DURABILITY: define a Windows creation-time security-descriptor/DACL contract and its inheritance/overwrite behavior; do not transliterate POSIX 0600 or apply permissions after writing as a substitute. Inspect effective rights/descriptor on disposable created files before payload publication, with negative inheritance and replacement cases. No privilege enablement, global ACL/trust/certificate change, registry setting, driver or developer-mode change is authorized. Missing reparse-creation rights makes that oracle Not assessed, not skipped PASS. Distinguish file-content flush, namespace publication and supported metadata durability using primary contracts and measured failures. The existing SaveResult documentation describes file and final-directory fsync; Windows may not silently reuse DurabilityConfirmed with weaker semantics. W0 must propose the exact equivalent or a typed contract decision with truthful uncertain/not-confirmed result. No hardware power-loss guarantee can be inferred from successful flush calls or a CI run.

SPIKE AND HARNESS: create a standalone net10.0 spike pinned to the repository toolchain, not a production fallback. All effects stay in a unique task-owned scratch root and disposable fixtures. Build locally with explicit artifacts path and task-local DOTNET_CLI_HOME/NUGET_PACKAGES; retain macOS compile/source evidence separately from Windows execution. Non-Windows execution explicitly refuses native qualification. Design structured output binding source/SDK/OS/architecture/filesystem, executable hash, named scenario, action/result/error, file identity and before/after hashes plus publication/durability state. Windows job/process containment must be established before launching long-lived children: use a documented ownership primitive with measured child-and-grandchild cancellation, PID/start/executable readback and absence confirmation. A parent exit alone is not quiescence. Include timeout and observer-failure negatives; no broad taskkill or scanning/killing unrelated processes. Missing observation is Not assessed/nonzero. No inherited user HOME replacement or global cache mutation.

FINITE ORACLES: enumerate a fixed required matrix for create/read/overwrite, conflict and aliases, denial, cancellation, injected write/replace failure, owned-only cleanup, ancestor/leaf redirection and process lifecycle; distinguish expected rejection, unsupported capability and pass. Use independent retained fixture bytes and wrong-result negatives so the receipt parser fails on missing/duplicate cases, wrong source/binary binding, unsupported masquerading as PASS and live-child claims. W0 may leave native cases unexecuted, but their source and expected outcomes must be reviewable. The termination variant is unresolved API/invariant mappings and required fixture/receipt contracts; after one evidence-directed local correction, an unresolved primitive or exhausted budget yields a precise checkpoint, not an invented Windows result or automatic continuation.

CI PREPARATION ONLY: author the exact reviewable Windows x64 qualification workflow and local driver, using one explicit hosted runner label, pinned SDK and reviewed action revisions, minimal contents-read permissions, no secrets/OIDC, no persistent caches or deployment, finite timeout and retained source-bound logs/receipts. Restrict execution triggers to an explicitly reviewed manual/disposable-branch route; do not enable new main/PR-wide product runs by accident. Use task-local temp/output and telemetry/certificate-generation opt-outs. Record expected hosted-runner cost class from current official policy, but actual usage/spend remains Not recorded until executed. No push, dispatch, VM provisioning or purchase is authorized by W0. This is a stage boundary, not a new human permission demand: after independent review, root/Owner may authorize the concrete branch/workflow run under the user's existing testing intent, with verified access, cost and permissions. Do not wait on that later decision to finish local W0.

HANDOFF: deliver clean exact-path commit, design with API/invariant/security table and unresolved decisions, executable source, raw local receipts and proof clearly distinguishing Prepared/Compiled/Executed/Not assessed. Root independently applies Data, Security and Test vetoes to ABI, DACL-at-creation, races/reparse containment, durability meaning, failure oracles, workflow permissions and child cleanup. Owner then decides W1's exact measured dispatch packet and W2 production paths from actual evidence. W0 author cannot clear its own veto. No production store or C implementation, canonical product join, Windows runtime PASS, interactive accessibility, on-screen timing or M1 acceptance follows. Native R39 review and separate approved M1.1 planning remain independent.

- request: req-01M3BBM9JMB1GHXVKT1W1RDSC1 · ruled by: cfd-owner-20260923 · at: 2026-09-25T04:04:31Z

### Ruling 41 — Prepare a truthful visible-endpoint spike without alternate capture execution

DECISION: choose A for one bounded isolated measurement-contract spike, at most 60 calls/40 minutes, not performance trials or a product metric change. User-approved M1 includes Windows x64 runtime and actual visible budgets; full section authoring belongs to a new session and is not authorized here. No 16 GB reference-host/full-workload availability is required to investigate endpoint feasibility, but both remain acceptance prerequisites.

ROUTE AND EXACT SCOPE: Coordinator assigns one fresh requested gpt-6-astra author under the existing observed-only isolated-worktree/cancellation/path controls; keep requested and effective model distinct, with Not recorded where identity is not exposed. At most root+Coordinator+one author active. Exactly seven authored files: docs/design/visible-presentation.md; docs/proof/visible-presentation.md; tools/spikes/VisiblePresentation/VisiblePresentation.csproj; tools/spikes/VisiblePresentation/Program.cs; tools/spikes/VisiblePresentation/Capture.swift; tools/qualify-visible-presentation.py. This is seven: the csproj, Program and Swift helper are three separate files. No production source, current C/W0 paths, framework fork, dependency upgrade, renderer override, injected global input or product telemetry change. Official derived/audit files follow designated ownership. Source/package baselines and proof status must be explicit.

AUTHORIZATION BOUNDARY: root reread the supported CUA contract: all UI actions use cua_repl; other computer-interaction technologies require the user's specific request. A ScreenCaptureKit observer is alternate screen observation. Therefore this packet permits helper source preparation, compilation and noncapture contracts/tests only, NOT capture execution, screenshot/window enumeration, alternate UI control, TCC prompting/reset/grant, settings changes or permission inheritance from CUA. A supported nonprompting permission-status preflight may be prepared and documented; any actual capability query must remain noncapture/nonenumerating and within the reviewed tool boundary. Existing OS permission alone does not supply the missing user authorization. Root first independently reviews an exact target-only helper and privacy/lifecycle contract; only then batch a concrete specific user authorization request with any necessary one-time OS permission and unavoidable host information. Do not ask prematurely or bypass a denied/unavailable tool. Root retains all native UI operation ownership.

GROUNDING: joined packet 582373d was independently reviewed as research, not runtime proof. Owner read its primary-source ledger and installed Apple headers: ScreenCaptureKit supplies a mach-absolute WindowServer frame-event timestamp; Metal presentedTime is host time and can be zero for skipped/unpresented frames. Current R39 MainWindow correlation ends at a fresh compositor batch and Program.ManagedStartTicks begins inside Main. Avalonia's pinned initializer tries OpenGL/Metal/Software; neither the comment nor option order proves the running backend. Compare supported accessible renderer presentation information with the prepared capture observer. Record the actual selected backend when it can be observed through a supported in-process contract; otherwise mark it unobserved. Do not select Metal merely to get a timestamp, access a private drawable by guessed reflection or call an inaccessible callback a qualified route.

FIRST DELIVERABLE: a precise endpoint/receipt contract and standalone synthetic three-region target using the pinned application framework, clearly separated from production. Model inspector/status/canvas regions with deterministic operation/source/draft-generation witnesses; a marker or log line alone cannot prove the content of all required regions. Compare feasible supported backend observation and capture contracts before committing to either. Noncapture tests can establish deterministic region-state and receipt-parser behavior, clock sampling/conversion code, timeout/cancellation/lifecycle and injected-event negatives. An unexecuted native observation is Prepared or Compiled, never Verified runtime or PASS. Any synthetic signal remains synthetic and cannot qualify the application backend or visible endpoint.

CLOCK AND STATE OBLIGATIONS: define each clock domain/epoch/unit and monotonic behavior explicitly, including Stopwatch, mach absolute, presentation-host seconds and external process-start boundaries. Retain paired samples, conversion/resolution/drift uncertainty and raw event times; callback receipt time is a separate quantity. Define input boundary without claiming OS-input latency from direct method calls. Define cold launch before process/runtime creation, not Main. A final-state predicate must identify the intended accepted/draft generation in all required regions and distinguish Assessing, partial update, accepted old content, preview and Cancel acknowledgement. Specify target process/window/source identity, display/scale/visibility convention, frame status and sample loss. Window capture can produce content without proving a physically visible unoccluded screen; no such implication is allowed. WindowServer/drawable events are OS presentation evidence, not measured panel-photon response.

INTERVAL MODEL: do not adopt the packet's candidate latency interval merely from a formula. Prove the temporal premises: ordered calibrated observations, known nonfinal predecessor, final-state persistence/monotonicity over the operation interval, and a correct input bound. A transient earlier final frame followed by regression, unknown dropped frame, stale generation or missing predecessor cannot silently strengthen a lower bound. Gaps widen uncertainty or produce Not assessed. Specify upper-bound PASS/lower-bound FAIL/overlap inconclusive logic and how failed, timed-out and unobserved trials remain in denominators before any future sampling. Current thirty synthetic trials and three managed starts are historical diagnostics, not the future protocol or a population-tail proof.

FINITE NEGATIVE CONTROLS: freeze a source-bound matrix covering wrong process/window/package, stale source/draft/operation, duplicate or reordered events, one of three regions still old, misleading completion marker with wrong content, unchanged/skipped/zero presentation, blank/suspended/incomplete frame, unknown clock conversion/drift, negative delta, missing frame/predecessor, timeout, hidden/occluded-unproved target and late Preview after Cancel. Validate clock/state algorithms with deterministic independent fixtures and wrong-result controls now. Prepare a future known-delay experiment whose presented bound must move, not merely CPU/callback duration, plus observer-overhead assessment. Label each control as synthetic-executed or native-unexecuted; do not call a synthetic delay a measured screen result. Preserve raw receipts and expected failures.

TARGET-ONLY CAPTURE DESIGN: the reviewable helper must limit any future authorized observation to one exact-owned synthetic/review target, avoid other applications or full-desktop content, use finite duration, local-only task scratch and no egress, stop/release stream and children on timeout/cancel, and retain only the minimal region evidence needed for the oracle. Describe unavailable authorization, empty/blank capture and exact target mismatch as explicit refused outcomes. No fallback to broader capture or another UI technology. Prepare the exact proposed launch command, binary/source hashes, permission preflight semantics, retention policy and cleanup evidence for root review and the later user request. No credentials, global trust or capture-consent workaround.

BOUND/HANDOFF: the declining set is unresolved endpoint/API/clock/predicate contracts and the fixed negative cases, not repeatedly adjusted thresholds. One coherent implementation and one evidence-directed local corrective pass are allowed within the total cap; unsupported runtime seams or cap exhaustion yield a precise checkpoint and released leases. Use task-local build artifacts/caches, current certificate/telemetry opt-outs and owned PID/start/absence tracking; no broad process termination. Deliver clean seven-path commit, design, proof, raw noncapture receipts and explicit comparison of accessible/unavailable seams. Root independently reviews Test/SRE/Data/privacy/accessibility implications before any concrete alternate-capture authorization request or production instrumentation seam. No CI dispatch, W0 gate, product latency PASS, renderer replacement, Windows/native UI acceptance, C join or M1 completion is granted. Full section editor design/implementation remains stopped for a new session.

- request: req-01M3BDN7535JSDPJ740MM5838G · ruled by: cfd-owner-20260923 · at: 2026-09-25T04:41:35Z

### Ruling 42 — Repair the Windows qualification driver's five portable-text findings

DECISION: choose A for one bounded same-author mechanical portability correction in a fresh isolated session/worktree from the recorded integrated refusal HEAD 12a6402. At most 20 calls/15 minutes. Author exactly tools/qualify-windows-runtime.py; no workflow, production, native-spike, timing or section files. Retain the existing qualified R40 author route and identity/process/path controls; no model/probe loop. Coordinator assigns the exact file lease and records starting cwd/branch/HEAD. Full section work remains reserved for a new session.

EVIDENCE: Owner read the typed request and current driver: four write_text calls at lines 397,425,431,490 specify UTF-8 but omit newline, and __main__ prints without a guarded stdout/stderr reconfigure. Root independently reproduced the same five findings; conductor step8 refused the join while the other ten gates passed. The installed docs/ai-forward-pack/scripts/verify-portable-text-io.py defines this as existing PLAT-A, not a new platform rule. W0 82a366a and root review f080076 remain valid only for their frozen bytes; no corrected-source hash is inferred from those receipts.

REPAIR: preserve the observed RED receipt, then add an actual LF newline parameter to all four existing text writes and the established guarded UTF-8/errors=replace stdout and stderr reconfiguration before CLI output. Preserve non-reconfigurable/closed-stream handling as the existing portable pattern does. No literal backslash-n string in place of LF, output/schema/exit-status change, suppressed exception, missing-case acceptance or Windows-platform guard weakening. Sweep this one file for the same text-write/console class. This does not require a new generic I/O abstraction.

PROOF: run the applicable existing portable-text and subprocess-UTF8 gates plus the driver's existing --self-test on corrected bytes; read the named results and retain source hash/stdout/stderr/exit evidence. The portable gate must transition from the five source findings to zero; self-test must still report its actual negative-control result and native qualification Not assessed. Do not execute the driver default full native-spike build merely to repair newline/stdio. A targeted wrong-result or legacy-console check may be used if the existing controls leave a concrete gap, without expanding authored paths. One evidence-directed local correction is allowed; unexpected behavior, remaining failure or budget exhaustion stops with precise evidence, not a waived gate.

MISSED-GATE CONTROL: Coordinator records the recurrence as applicable tooling portability gates omitted from a local spike handoff, linked to PLAT-A and the exact five findings. The control is a handoff checklist/verification declaration requiring the existing portable-text and subprocess-UTF8 gates for new or changed Python tooling before it enters conductor, with retained execution output; check-docs alone is insufficient. Do not invent a duplicate detector or record only a prose lesson. Coordinator owns any plan/register update outside the one-file author scope.

INDEPENDENCE/JOIN: root performs a bounded delta and corrected-byte/receipt review, not a repetition of unchanged Windows API research or native fixtures. If accepted, Coordinator performs one supported integrated conductor/11-gate retry and preserves the initial refusal as historical evidence. An integrated failure remains a stop. No Windows runtime PASS, native product admission, C/M1 join, capture authorization, branch push or W1 dispatch is granted. The reported remote branch/access facts can support a separate concrete W1 decision after corrected-source review and integrated gates; they do not widen this one-file request by inference.

- request: req-01M3BE2YB1VMCRRKCK9PFJF8PB · ruled by: cfd-owner-20260923 · at: 2026-09-25T04:46:15Z

### Ruling 43 — Authorize one exact-branch Windows qualification run and require live-leader preflight

DECISION: choose A, conditionally authorize one disposable Windows x64 W1 qualification push/run after the concrete preflight below. This is within the user's existing repository-testing intent and approved Windows-in-M1 scope. It does not authorize production admission, a release, a default-branch change, alternate capture or full section work.

EXACT REF AND CHANGE: Coordinator creates a fresh isolated worktree on feature/windows-w1-qualification-20260925 from corrected integrated HEAD 6c3a3742d7e6b72142d192497ddbab9e769f93e1 (later shared decision/audit-only ancestry may be included if its source equivalence is recorded). Exactly one authored execution change is permitted: .github/workflows/application-windows-qualification.yml replaces its dispatch-only trigger with push restricted to that literal branch and changes the job guard to refs/heads/feature/windows-w1-qualification-20260925. No wildcard, PR/default-branch trigger, schedule, extra job, product edit, source/driver fix or permission expansion. The W0 C#/Python source must stay at the independently reviewed corrected bytes. Coordinator owns this route edit/proof under exact lease; root independently checks the final diff before the push. No new model/probe route is needed for this mechanical workflow step. Coordinator's existing route/proof registers may record the run via their normal ownership.

PRECONDITIONS: preserve root W0 review f080076 and the corrected-byte addendum for 529bcb9; the actual joined source has completed 11/11 integrated gates. Recompute the final five-input manifest including the changed workflow, verify clean branch/HEAD and unchanged corrected qualifier SHA256 4a0ea88907fcd8ff58ba38b1846af7b6ee1a488fc4412b97f55b671b1d4a724d, and bind the exact commit root reviews. The old native receipt cannot be relabelled for the changed workflow/driver. Run only the applicable workflow/docs/portability validation necessitated by this delta; no redundant local native-spike full build. Inspect that existing docs-health remains main-only and no other event is unintentionally enabled. Before publication, reconfirm origin is timianmalloo/CFD-Workbench, repository public, existing push access and Actions enabled, and the exact remote branch absent. Owner directly verified these metadata/absence facts in this ruling run; repeat only the immediate mutation preflight. Any mismatch stops; no force-push, retargeting or access changes.

WORKFLOW/COST BOUNDARY: retain windows-2022, SDK10.0.203, the three reviewed full action SHAs, contents:read, no secrets/OIDC, checkout persist-credentials:false, task-local SDK/caches/artifacts, telemetry and certificate-generation opt-outs, 12-minute job timeout and 3-day artifact retention. Do not enable paid/larger/self-hosted runners, install a desktop VM or change billing. Current official GitHub runner policy lists standard Windows x64 jobs for public repositories as free; this supports the bounded route, not a measured usage/charge claim. Record actual billed usage only if exposed, else Not recorded. Preserve explicit output file globs and synthetic fixtures; do not upload credentials, environment dumps or arbitrary filesystem contents.

EXECUTION: after root's exact workflow/source preflight PASS, push precisely the new branch once to origin with no tags, other refs, force or default-branch update. No PR or separate workflow_dispatch is needed. GitHub's documented push event supports workflows not on default, whereas this unregistered dispatch-only workflow did not provide a first-run route. Observe exactly one matching workflow run bound to the pushed SHA/ref; record URL, ID/attempt, workflow hash, runner image/version/architecture, SDK and all raw statuses. Allow at most 30 minutes total observation including queue time, with the workflow's 12-minute execution cap unchanged. If no run appears, a permission/queue/checkout failure occurs, runtime fails or the deadline is reached, retain evidence and stop; no second push, rerun, edited-code retry or alternate image without a new concrete decision. Cancellation of only this owned run is authorized on timeout/unexpected activity, with observed conclusion recorded. Do not infer child quiescence from parent/job disappearance.

EVIDENCE AND REVIEW: download/retain the exact job logs and narrowly declared artifacts before expiry; independently hash raw receipts, source/binary manifest, fixtures and before/after results. Inspect individual cases and process/descendant cleanup, not just job color. Directory durability is deliberately unresolved and may leave native qualification Not assessed/exit3; preserve that result rather than muting it to manufacture green. Partial/no artifacts or an unobserved timeout cleanup remain explicit gaps. The reviewed hosted UIA root/session capability probe may execute as part of this disposable-host qualification; its result describes that environment only. UIA root availability or userInteractive=true does not establish the product window, native dialogs, Narrator, successful display presentation or the reference laptop's timing. Root independently applies Data/Security/Test review to actual Win32 ABI/DACL/sharing/race/process results, and Owner receives a bounded W1 handback before any W2 implementation scope.

PRODUCTION HOLDS: the prior same-object reparse/absolute-name containment question, final-directory durability equivalence, foreign ACL/overwrite policy and exact product identity/history/recovery remain open until separately proved and ruled. No macOS guarantee or POSIX permission numeral is transferred by analogy. Neither a successful hosted run nor its 16GB nominal memory proves physical reference-device presentation or W3 native accessibility. Preserve the disposable remote branch and local receipts for review; no automatic deletion or product merge.

LIVE-LEADER CONTROL: immediately before every conductor invocation capable of mutation, the designated Coordinator uses supported leader renew, then leader who, and records that state is live with its own session holder and the expected current epoch. Recheck exact worktree/HEAD/join plan after waiting on any decision/merge. An expired or mismatched designation is not repaired by assuming the old epoch still leads: use supported release/reclaim/designation rules and quiet-period requirements, then repeat preflight. Begin only with enough remaining lease for the bounded operation or a supported renewal/monitoring plan. If leadership is lost during a running operation, preserve its evidence, stop further mutations at the first safe boundary and resolve the leader condition before continuation; a successful test remains evidence but cannot conceal the governance lapse. Do not rewrite managed conductor or infer a code bug from its narrower epoch fence. Coordinator records epoch17's lapse as a missing live-designation preflight and adds the renewal/readback receipt as a mandatory join-packet control. Register/ruling appends and conductor merges stay serialized.

HANDOFF: one final frozen workflow commit, one push/run record and independent root result; no launch or dispatch is performed by Owner here. All C/native/timing/M1 gates remain as actually evidenced, and the user's instruction to stop before full section editor remains in force.

- request: req-01M3BEYXXM2WTK5RPH9VC1PBMX · ruled by: cfd-owner-20260923 · at: 2026-09-25T05:02:35Z
