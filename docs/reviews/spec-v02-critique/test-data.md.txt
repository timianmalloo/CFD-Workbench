# Critique — specification revision 0.2 · Test Architect + Data & Persistence Architect (Adversary Mode)

Artifact: `/Users/mallalieut/projects/CFD-Workbench-feature-knowledge-experts-spec-v1/docs/specs/cfd-workbench.md` (rev 0.2, 593 lines). Line numbers below are from that file.
Evidence base read: KB index (full: 31 headlines, contradiction register, KB-1…KB-24); area 02 (headlines, §8 payload, invariants, implications); area 05 (headlines, §6 native format, implications); area 10 (headlines, run_key + invariants, implications); area 13 (headlines, GAP-09 numerical testing, implications); area 07 (fixture table, implications 1–11); area 11 (§9 provenance strip, implications); DPA card; DM1–DM18.
Budget: 16 tool calls of ~35. Stories sampled: 46 of 82 (all DOC, GEO-01–14, CAT-01–03, ANA-01–18, CFD-01–06, VIZ-01–04, EXP-01–02, AI-01–06, CLI-01), all UX-01–10, all UI-01–14, all 13 A7 rows.

---

```
PERSONA: test-architect   MODE: Adversary   TIER: T2
VERDICT: BLOCK
FINDINGS:
  - [Blocker] (Verified) A6 line 266 defines the label "Verified numerical implementation" and says only "the last two require their specific proof"; A2 line 60 makes S5's release proof "One validated single-phase case on each supported platform". Neither names the proof. No fixture, tolerance or CI ring is attached to the label anywhere in the spec (grep: "metamorphic" 0, "manufactured" 0, "elliptic" 0, "GCI" 0, "Verified numerical" 1 occurrence — the definition itself). A correctness label with no verification path is exactly my hard veto.  evidence: KB-10 ("Labels are mechanical … earned per method by a CI ring-0 fixture suite"), KB-23 (the floor list), area 13 implication 1 and area 07 implication 1 ("kept as tests, not prose — CI6").  fix: v1 A6 states the gate per label: *Verified numerical implementation* = per-method ring-0 suite listed by name (geometry: manufactured elliptic/trapezoidal planforms with closed-form S/centroid/inertia + observed-order on station refinement; knot-insertion ≤ round-off; reparametrisation, station-insertion, scale-invariance, mirror and unit-round-trip metamorphic tests; FsCheck/proptest invariants. 2D: thin-airfoil 2π, Joukowski/Kármán–Trefftz Cp. Wing: flat-plate 2π at AR→∞ within 1 %, elliptic e→1 with CL rel 1 %/CDi 5 % proposed, symmetric section at α=0 Cl=0 abs 1e-6) + tolerance-based cross-platform golden masters with platform/runtime/commit provenance on both-OS ring-0 runners; *per CFD case* = reported GCI per ITTC 7.5-03-01-01 Rev 05; *Experimentally compared* = dataset id + configuration comparability + E = D − S with U_V. "Validated single-phase case" in A2 becomes "NACA 0012 TMR case (tripped data for fully turbulent, untripped for transition model) with GCI on each platform".
  - [Blocker] (Verified) CLI-01 line 262: "numerical output … agree to the operation's published tolerance". No tolerance is published anywhere in the spec (A7 Compatibility line 279 says only "opens on both platforms"). A parity claim with no tolerance has no failing input — any two outputs "agree" to an unstated tolerance.  evidence: KB-22 (bitwise only for + − × ÷ √ FMA; transcendental functions platform-dependent), area 13 implication 10 (publish per-operation tolerance: bitwise for the exact-rounded subset, rel 1e-12 where transcendental functions occur; both-OS runners in ring 0; open question 7 — RyuJIT FMA contraction spike decides it).  fix: CLI-01 states the two-class tolerance table (bitwise / rel 1e-12), requires the CLI to print the run_key it evaluated so CI asserts GUI/CLI identity by key equality (area 10 implication 13), and A7 Compatibility adds "ring-0 on macOS ARM64 and Windows x64 runners on every push".
  - [Major] (Verified) DOC-02 line 206, GEO-06 line 214, GEO-11 line 219, GEO-14 line 222, EXP-01 line 254 all assert "within 1 µm" but never say what is compared (JSON text? sampled surface points? which sampling? max or RMS?) nor pin the evaluator. Area 05 implication 6: serialisation contributes 0 µm, so the test must re-evaluate the loft and compare surfaces. Area 02 §8 rule 2: "GEO-14's … recover within DOC-02 tolerance is only testable if the evaluator is pinned" — same controls under a different solver version give a different curve. Without the oracle statement the tests pass on JSON equality (Coverage Theater).  evidence: KB-1 (three named tolerances: identity 1 µm abs AND 1e-6 rel, model/join 10 µm, angle 0.1°); DOC-02 states only the absolute half.  fix: one shared "identity oracle" paragraph: re-evaluate from the reloaded definition with the pinned evaluator id+version; compare N surface samples (state N and the sampling rule) and every station readout; pass iff max |Δ| ≤ 1 µm AND ≤ 1e-6 relative; every criterion that says "within 1 µm" cites that paragraph.
  - [Major] (Verified) GEO-06 line 214 accepts ≤ 1 µm for a promotion that is a knot insertion (loft rule B) or an added satisfied constraint (rule A). The KB executed it at 6.9e-17. A mutation that perturbs the surface by 0.5 µm on promotion passes GEO-06.  evidence: area 02 headline 2, implication 5; KB-23 "knot-insertion exactness (≤ round-off)".  fix: GEO-06 Then: "deviation reported and ≤ round-off (1e-12 relative); the identity tolerance is not the acceptance bound for insertion".
  - [Major] (Verified) GEO-02 line 210 is the only geometry oracle and it is a rectangle and a linear taper — both exact for any chord integration, so it cannot fail a wrong quadrature rule or a wrong span parameterisation. No manufactured geometry, no observed-order test.  evidence: KB-23; area 13 GAP-09 "manufactured geometry"; area 02 implication 11 (Gauss–Legendre per span with Richardson "±").  fix: add GEO-02b: exact elliptic planform (S = πab/4-class closed form), centroid and second moment; observed order on station refinement matches the design order; reported "±" bounds the true error.
  - [Major] (Verified) ANA-04 line 229 (VLM/strip "reports loading, induced drag, computed span efficiency…") and ANA-01 line 226 have no analytic oracle: any VLM that returns numbers passes. The only numerical fixture in ANA is ANA-17's q·S·CL arithmetic, which the spec itself calls "a formula test, not a predicted foil".  evidence: area 07 fixture table (flat plate 2π, elliptic AR 8 α 5° → CL 0.439/e = 1.000, e > 1.005 is a defect, twist/anhedral signs, symmetry invariant); KB-23.  fix: ANA-04 gains a Given/When/Then per fixture row with its tolerance; Warren-12 and Bertin–Smith stay out until their numbers are re-established (Flagged in KB).
  - [Major] (Verified) ANA-05 line 230 and ANA-12 line 237: "lift = target within 1 %". The depth-only free-surface correction is 2–5 % at goal-state depths and the measured Froude loss ≈ 17 %; the criterion is unfalsifiable as written because it does not say whether the correction is applied, and cannot be met if it is.  evidence: KB-12 ("Find operating α states whether it was applied because the depth-only correction … already exceeds the 1 % load tolerance"), headline 10.  fix: Then adds "with the free-surface layer named (applied: h/c, Fr_h, correction id | not applied: deep-water label shown)"; the 1 % is the root-finding tolerance on the chosen model, stated as such.
  - [Major] (Verified) ANA-07 line 232 freshness has no discriminating input. A timestamp/dirty-flag implementation passes every Given in the row and fails on: Undo back to the identical revision (should be Current again), reopen after save (timestamps lost), method version bump with identical inputs (should be Historical).  evidence: KB-13 "freshness by content-hash key equality … never by timestamp"; area 10 run_key invariants (i)–(iv).  fix: add three Givens: "Given Undo to a revision whose key equals the run's key, Then the run is Current"; "Given save/reopen, Then freshness is unchanged"; "Given the method version changes with identical inputs, Then the run is Historical".
  - [Major] (Verified) CAT-02 line 224 names Selig/Lednicer but no fixture; "ambiguous" does not cover the Phase-0 defect (a Lednicer file read as Selig is not ambiguous — it parses to a plausible wrong shape).  evidence: headline 16, area 05 implication 1 (e817 in both layouts, `#` comments, CRLF, BOM, unequal counts, Lednicer-as-Selig rejected as self-crossing, half profile).  fix: CAT-02 Then: "the same airfoil in both layouts yields the same normalised profile within identity tolerance (differential test); the named corpus is a required fixture; a Lednicer file forced through the Selig path is rejected as self-crossing".
  - [Major] (Verified) CFD-03 line 246 "the process tree terminates" is tested only on a host process; the KB shows `Process.Kill(entireProcessTree)` silently skips containers/WSL, so a host-only kill passes the test and leaks a solver.  evidence: headline 20; KB-16 "cancellation addressed to the substrate".  fix: Given per substrate (native binary, Docker, WSL): Then no solver process survives cancel, verified by substrate-side process listing; A7 Reliability's "≤ 5 s or visible timed-out termination error" is measured per substrate.
  - [Major] (Verified) ANA-15 line 240 "Ncrit preset initially proposes 2.0" contradicts the KB and produces a single polar where the evidence demands a band; a test of "one polar at Ncrit 2" would pass a product that hides the uncertainty the KB documents.  evidence: headline 15 ("band 2–4 run as a pair, not a constant"), KB-9 (Ncrit pair + surface state clean/tripped as a band).  fix: ANA-15 Then: "polars are computed at Ncrit 2 and 4 and displayed as a band labelled 'practitioner range; no measured water N-factor'; every polar carries surface state (clean / tripped with k_s)".
  - [Major] (Verified) AI-06 line 261 changes the model identifier with no re-evaluation requirement; A7 line 294 lists five evals where the KB harness has ten and none is tied to the model-change event. Prompt/Schema Drift Without a Gate (owned anti-pattern).  evidence: KB-22 ten-item harness incl. "drift on model change"; area 10 implication 15.  fix: AI-06 Then: "the ten-item eval suite is re-run and its thresholds met before the new identifier is selectable"; A7 names all ten (thresholds still deferred to architecture, but the items are not).
  - [Major] (Inferred) ANA-11 line 236 "components reconcile with the reported total to the solver tolerance" and CFD-02 line 245 "failed mesh-quality threshold" reference tolerances/thresholds that no row states; snappyHexMesh defaults are Flagged in the KB.  evidence: KB confidence summary (snappyHexMesh thresholds Flagged); A4 line 181 "to its declared numerical tolerance" — declared where?  fix: name the reconciliation tolerance as a per-run manifest field the test reads, and name the mesh-quality metric set (the ITTC floor per KB-16) as the fixture.
  - [Minor] (Verified) Threshold-less adjectives: DOC-01 "work offline" (which post-condition?); GEO-02 "to displayed precision" (unstated); GEO-05 "any continuity break is marked" (KB-4: continuity is measured per knot with angle/curvature tolerances — spec never gives the value; A4 line 143 "declared evaluator tolerance" is never declared); GEO-08 "fit error above tolerance" (value unstated); GEO-13 "to the declared tolerance"; ANA-02 "sparse reconstructed Cp"; VIZ-01 "perceptually uniform legend" (name the map: batlow/cividis per KB-20); ANA-17 "≈17.142857" in a formula test (state exact digits).  fix: each cites KB-1's three tolerances or a named value.
  - [Minor] (Verified) GEO-13 line 221 tests one weight step (1→2). The executed property is monotone convergence without reaching the control at any finite weight; a single step cannot catch a non-monotone solver.  evidence: area 02 headline 3 (gap 0.0385→…→0.0000 for weights 1→1000).  fix: add a property test: for weights w1<w2<…<wn the gap is strictly decreasing and > 0.
  - [Minor] (Verified) UX-01 "≤1 action", UX-03 "Every error above" (unenumerated), UI-01 "cannot be mistaken for editable fields" (no observable), UI-05 "no continuous decorative motion" (definitional).  fix: define "action" (one pointer or key event); enumerate errors from C2's state table; UI-01 becomes "derived fields expose a read-only role in the accessibility tree and the derived token".
  - [Minor] (Verified) A7 table: measurable rows with a fixture — Performance (fixture named; machine deferred, honestly), Reliability (fault injection, ≤250 ms/≤5 s), Accessibility (HTML). Rows without a fixture or number: Compatibility (no both-OS runner, no tolerance), Security ("parser size limits published" — no value; no fuzz corpus though area 13 names one), Privacy (no egress-monitor test), Maintainability ("regression controls for signs, units…" — no fixtures named), Safety (KB-19 names three fixed-copy points, spec says "relevant points"), Observability (no assertion that one event is emitted per operation). "orbit ≥30 fps" lacks duration/percentile.  fix: each row gains a named fixture or is labelled "boundary, not requirement".
  - [Minor] (Inferred) UI-10 line 533: the prototype's "in-artifact check" verifies itself — a page reporting its own contrast is a test-of-a-mock unless an external runner (`ui-craft-gate.py`) is the gate.  fix: UI-10 names the external gate as the measurement; the in-page check is a smoke signal only.
  - [Nit] (Verified) A5 preamble line 201 "Every row is a future product test" — the spec must also require red-before-green: each acceptance test is observed failing against a stub before implementation and the failing run is in the Proof Pack; otherwise the suite is Unverified Green by construction.
CLEARS-THE-VETO: no — traced tests? partially (46 sampled; 2 Blockers, 11 Majors above have no failing input or no stated tolerance); red-before-green? not required by the spec; Proof Pack attached? none (design-time review; the spec must mandate one per story).
RESIDUAL RISK: 36 stories not sampled line-by-line (spot-checked only); KB-23 floors absent from the spec entirely (manufactured geometries, knot-insertion exactness, metamorphic, property, analytic oracles, TMR+GCI, DAT corpus, both-OS ring 0) — a green suite built from rev 0.2 as written proves arithmetic and UI states, not geometry or hydrodynamics; the seven load-bearing Flagged KB claims (board speeds, TE floors, Warren-12/Bertin–Smith, snappyHexMesh thresholds, H105) cannot become fixtures until closed.
```

---

```
PERSONA: data-persistence-architect   MODE: Adversary   TIER: T2
VERDICT: BLOCK
FINDINGS:
  - [Blocker] (Verified) Two stated integrity invariants cannot be enforced by the model as written. A3 line 100: Surface revision — "Exactly one definition determines the supported surface" — but the owned concepts omit loft rule id, blend rule id, evaluator id+version, tolerances and frame; the same controls/weights under a different evaluator version yield a different surface (area 02 §8 rule 2). A3 line 104: Analysis run — "A result always identifies the exact inputs, reference quantities and method that produced it" — but the owned list has "method, settings" with no method *version*, no backend build digest, and "input snapshot" instead of identity+hash; a method-version bump with identical inputs is indistinguishable (area 10 run_key invariant iii).  evidence: KB-1 ("the record is knots + control points + constraints + rule + evaluator version"), KB-13/area 10 run_key definition, area 05 §6 (`revision_hash` over the JCS form of the surface-revision subtree).  fix: A3 Surface revision owns {loft rule, blend rule, evaluator id+version, tolerance set, frame}; Analysis run owns a **run manifest** {input revision ids + content hashes, fluid-property revision, operating point, reference quantities, method id + version, backend build digest, settings hash, DRC outcomes at run time, output artifact hashes}; freshness = key equality (ANA-07). Conceptual level only — no key types.
  - [Blocker] (Verified) Grain is inferred, not declared, in a domain with correction (retry). A3 line 105 Sweep: "one explicit selected attempt outcome or a pending/missing/cancelled state; retries retain prior attempt provenance" — two readers would produce different row counts (one per pair, or one per attempt). No grain exists for polar samples (the spec has no polar concept in A3 at all; "polar" appears only in ANA rows), field evidence, or discrepancy records. DM8 and my card: an inferred grain where rescheduling or correction exists is a Blocker.  evidence: area 07 implication 11 (polar sample keyed by profile hash, method+version, Re, Ncrit, α, water snapshot; free-surface-corrected value is a separate fact); area 11 implication 9 ("one row is exactly one reduced artifact of one sample"); KB-13 Discrepancy record per tier pair.  fix: A3 adds one grain sentence per fact: Polar sample — one row is one (profile revision, method+version, Re, Ncrit, surface state, α, water record) evaluation, recorded when the method returns; Sweep attempt — one row is one attempt of one (V, α) pair of one sweep, recorded when queued; "selected attempt" is derived (latest non-superseded), never a stored pointer; Field evidence — one reduced artifact of one sample attempt; Discrepancy — one (run_hi, run_lo, quantity, operating point). Additivity per measure: forces additive across strips only within one run; coefficients, ratios, σ, e non-additive; never summed across attempts.
  - [Major] (Verified) Operating point (A3 line 88) carries depth but nothing derived from it; "Froude"/"Fr_h" occur 0 times; ANA-13 reduces depth to a "tip-depth hint". KB says depth and Fr_h are mandatory inputs and σ, Fr_h, Fr_c, h/c, V_crit are derived from depth + the pinned water record, with missing depth ⇒ Unavailable and the deep-water label never silently applied.  evidence: headline 10, KB-5, KB-12, area 07 implication 5 (required fields incl. p_atm, target load W, Ncrit preset, per-station depth from geometry).  fix: Operating point VO = {V, water record ref, p_atm, h (reference point), α, β?, W, Ncrit preset}; derived read-outs {q, Re_ref, Re(y), σ, Fr_h, Fr_c, h/c, V_crit}; absence rule stated.
  - [Major] (Verified) Concepts the KB says v1 must carry that A3 lacks: **Goal state** VO (KB-5; spec line 71 only says a brief is intent), **Discrepancy record** (KB-13; "discrepancy" appears twice, both prose), **DRC outcome** with rule id/severity policy/located violation/exclusion-with-reason (KB-13; 0 occurrences — the spec's constraint failures, TE-floor flag and lock conflicts have no common shape), **Run manifest** (0 occurrences), **Profile design-point metadata** (KB-8: design Cl, design Re, intended σ range, source per field).  evidence: cited KB ids; DM14 (surface the drift).  fix: add the five concepts to the A3 term table with owner aggregate and invariant; Goal state is authored intent evaluated to a feasibility report, never a constraint.
  - [Major] (Verified) DM15 reader-with-no-writer: ANA-16 line 241 reads "a catalog's documented design Cl … absent design Cl is Unknown" but no aggregate holds design-point metadata. Writer-with-no-reader: Loft rule VO (A3 line 86) — the A/B rule is undecided (KB-3) and nothing in v1 writes it; Replay selection (line 93) is a viewing value that must be declared transient (not persisted) or it is dead weight round-trip tests will prove works; "optional context" surfaces (line 65, EXP-01) are CRUD passthrough — say so.  fix: classify each A3 concept as persisted-with-writer+reader / transient / passthrough; Profile revision gains design-point metadata (Unknown for Eppler/H105 until sourced).
  - [Major] (Verified) Two homes (DM7): (a) Profile revision line 83 owns "immutable normalized section geometry *and* its editable curve definition" — two representations of one shape with no authority stated; the KB makes the B-spline pair the record and coordinates a conversion with a measured residual (headline 7; area 02 implication 8). (b) Analysis run line 104 "input snapshot" vs line 109 "refer to revisions by identity, not copied competing truths" — contradictory in the same section. (c) Sweep line 187 stores "lists or inclusive ranges" and resolves values in preview — say which is pinned (the resolved (V, α) list is what the run_key hashes; the range is recipe provenance). (d) Reference quantities VO on a run duplicates S, c, b derived from geometry — acceptable only as a pinned copy checked against recomputation on load (area 05 `derived_check` pattern).  fix: one sentence each naming the authority and labelling the other as provenance/cache.
  - [Major] (Verified) History is asserted, not modelled. Line 109 "lossless history" and Profile revision "immutable" are the only statements; Design revision (line 76/103) has no parent link, no append-only rule, and its invariant "mutually consistent set" is undefined. Backend installation (line 94/106) "last smoke-test evidence" is Type-1 — overwriting it rewrites the provenance of every run that referenced "Ready" (DM10).  evidence: area 05 §6 `history[]` append-only {id, parent, kind, revision_hash}; KB-13 manifest pins "backend installation id and smoke-test evidence".  fix: Design revision = append-only node with parent reference and the content hash of the surface/profile revisions it references; smoke-test evidence is a fact row referenced by id from the run manifest; state the Type-2 rule for every attribute that a past run's meaning depends on (water record, method version, DRC policy, TE-floor policy).
  - [Major] (Inferred) Vernon rule (iv) — GEO-08 line 216 / A4 line 147: Apply "includes recipe detachment, profile assignment, all affected geometry and result invalidation in one undoable operation" modifies Profile revision, Surface revision, Design revision and Analysis runs in one transaction. Resolvable only if invalidation is derived (freshness by key equality needs no write) and the new revisions are appends: the transaction is one Design-revision append.  evidence: DM2 (iv); KB-13.  fix: A3 states "Apply appends exactly one Design revision referencing newly created immutable Surface/Profile revisions; freshness is derived and is never written".
  - [Minor] (Verified) DM4 leakage: A4 line 161 fixes the physical file extension `.cfdw.json` while line 32 and line 109 defer the schema to architecture; keep the name in the open-decisions table, not the model. DM18: A3 has no relationship/cardinality diagram (five Mermaid flows exist, no ER/class diagram).  fix: a Mermaid class diagram of the aggregates and identity references in A3.
  - [Minor] (Verified) KB-18 reserved vocabulary: the spec has "Not assessed" and the TE-floor setting (compliant with KB-18's exception) but lacks the glossary terms (load case, layup, manufacturing policy, fibre-angle sign convention), the relative-stiffness readout beside t/c (EI ∝ (t/c)³ reading existing geometry) and the documented beam-hook interface.  fix: add to A3 as reserved terms with "no field modelled until a consumer exists".
  - [Minor] (Verified) ANA-09 hashes: the spec never says what is hashed. Original DAT bytes: hash the bytes. Surface/profile revisions and run identity: hash the RFC 8785 canonical form, not the saved bytes (.NET shortest-round-trip output is not JCS); `null` for not-recorded, never NaN.  evidence: KB-14; area 05 headline 12.  fix: one sentence in A3 line 109's paragraph.
  - [Nit] (Verified) Polar fact must carry surface state (clean / tripped with k_s) as a key attribute, not a label (KB-9, headline 31) — otherwise clean and tripped rows collide on the same key.
CLEARS-THE-VETO: no — backward-compatible path? n/a at spec level (native format is declared additive, good); tested rollback? n/a; invariants enforced? no — two stated invariants are unenforceable without evaluator/method version identity, and the sweep/polar grain is inferred.
RESIDUAL RISK: the physical schema, migration and index plan are correctly deferred; nothing here checks query cost (sweep of 12×N attempts, polar tables at 10⁴ rows) or lifecycle (run/field-artifact retention and deletion are unstated — the spec has no deletion story for runs, sidecar fields or recovery revisions).
```

---

## MISSING (capabilities, concepts, flows, criteria, non-goals the KB shows are load-bearing)

1. **The label gate** — A6 needs the per-label proof (ring-0 fixture suite per method; GCI per CFD case; dataset id + comparability + E/U_V per experimental comparison). KB-10, KB-23, area 13 impl 1.
2. **Testing floors as requirements** — manufactured geometries + observed order; knot-insertion ≤ round-off; metamorphic set (reparametrisation, station insertion, scale invariance, mirror, unit round-trip, monotone depth); property invariants (area > 0, chord > 0, AR never stored, Cl(α) monotone in bracket, σ decreasing with V); analytic oracles (2π, Joukowski, elliptic e = 1, symmetric-at-zero); TMR NACA 0012 with GCI, tripped/untripped pairing; DAT corpus in both layouts + fuzz; both-OS ring-0 with tolerance-based golden masters carrying platform provenance. KB-23, area 13 GAP-09, area 07 fixture table.
3. **Published tolerance classes** — identity (1 µm abs AND 1e-6 rel), model/join 10 µm, angle 0.1°, cross-platform (bitwise subset / rel 1e-12). KB-1, KB-22.
4. **Operating point derivations** — Fr_h, Fr_c, h/c, σ, V_crit from depth + water record; p_atm; per-station depth; missing-depth rule. KB-5, KB-12.
5. **Goal state** value object (rider/equipment masses with labels, water, depth band, wind→speed band with source label, class-rule preset, operating-point set with weights and critical metric, objective, typed constraints, robustness rule) as authored intent + feasibility report. KB-5, KB-13 (headline 13's speeds are Flagged — label them).
6. **Discrepancy record**, **DRC outcome**, **Run manifest**, **Sweep attempt**, **Polar sample** as named concepts with grain. KB-13, area 07 impl 11, area 11 impl 9.
7. **Profile design-point metadata** and **polar surface state** (clean/tripped) and the **Ncrit pair** (2 and 4) as a band. KB-8, KB-9.
8. **Freshness by content-hash key equality** with the discriminating criteria (undo-to-identical ⇒ Current; method version bump ⇒ Historical; reopen preserves). KB-13.
9. **Cancellation per substrate** (native / Docker / WSL) as CFD-03 Givens. Headline 20, KB-16.
10. **Class-rule presets** (GWA, iQFOiL, Formula Kite with rule version/date) and the preset expansion from four to ≥ eight disciplines; sanity bounds as advisory DRC findings. KB-6, KB-7.
11. **Retention/deletion story** for runs, field artifacts, recovery revisions and attempts (unstated anywhere).
12. **Red-before-green and a Proof Pack per story** as a spec-level requirement (A5 preamble).
13. **Non-goals to state explicitly**: no averaging across tiers; no "cavitation-free"/"ventilation-safe" label; no single-number correction factor; no Oswald e as input; no surrogate promotion above the estimator; no bundled UIUC coordinates until written terms (CAT-03's "bundled supported polar" for Eppler presets conflicts with KB-14 — presets default to generated NACA sections). KB-8, KB-14, area 10 invariants 4 and 6.

## TIGHTEN (unfalsifiable criteria, two-meaning terms, claims stronger than the evidence)

1. "within 1 µm" (DOC-02, GEO-06, GEO-11, GEO-14, EXP-01) → one identity-oracle paragraph (what is re-evaluated, what is sampled, max-norm, abs AND rel, evaluator pinned); GEO-06 → ≤ round-off.
2. "published tolerance" (CLI-01), "declared evaluator tolerance" (A4 §Smooth, GEO-13), "acceptance tolerance" (GEO-08), "solver tolerance" (ANA-11), "failed mesh-quality threshold" (CFD-02), "to displayed precision" (GEO-02), "≈17.142857" (ANA-17) → the named number or the manifest field the test reads.
3. "within 1 %" (ANA-05, ANA-12) → root-finding tolerance on a named model, with the free-surface layer's applied/not-applied status in the Then.
4. "validated single-phase case" (A2 S5) → "TMR NACA 0012 with reported GCI on each platform".
5. "Ncrit … proposes 2.0" (ANA-15) → pair 2/4 as a band, labelled practitioner range.
6. "input snapshot" (A3 Analysis run) vs "refer by identity" (A3 line 109) → identity + content hash; "snapshot" reserved for the UI term "Run snapshot" (B1) only.
7. "normalized section geometry and its editable curve definition" (Profile revision) → B-spline pair is the record; coordinates are provenance with measured conversion residual.
8. "one explicit selected attempt outcome" (Sweep) → derived from the attempt facts, never stored.
9. "last smoke-test evidence" (Backend installation) → fact row referenced by id from the run manifest.
10. "mutually consistent set" (Design revision invariant) → "references, by identity and content hash, exactly one Surface revision and one Profile revision per authored station, and its parent Design revision".
11. "Every error above" (UX-03), "≤1 action" (UX-01), "cannot be mistaken" (UI-01), "perceptually uniform" (VIZ-01), "sparse reconstructed Cp" (ANA-02), "work offline" (DOC-01) → enumerated states, defined action, a11y role, named colormap, a station-count threshold, a named post-condition.
12. A7 rows Compatibility, Security, Privacy, Maintainability, Safety, Observability → a named fixture each or the label "boundary, not requirement"; "orbit ≥30 fps" → percentile over a stated interaction on the named fixture.
13. Two meanings of "revision": Surface revision (aggregate) and "revision" in the footer/B1 (Design revision node) — name the footer value "Design revision" everywhere.
14. Two meanings of "sample": a sweep (V, α) sample and the DOC-01 "Sample" design fixture — rename the latter "Sample design".
15. "depth" is used for the UI archetype facet (`Depth:Diegetic3D`, line 495) and for submergence h — keep the facet token verbatim but never call submergence "depth" without the symbol h.
16. A5 preamble "Every row is a future product test" → "every row has an observed-red test, traced in the Proof Pack, before its story is marked done".
